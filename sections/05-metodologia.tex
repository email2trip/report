\FloatBarrier{}
\section{Metodología Aplicada}%
\label{sec:methodology}

Para estructurar las fases de desarrollo del proyecto y la gestión de los objetivos específicos se aplicó una
combinación de \textit{Scrum} con \textit{Crisp-DM}.
Se detallan a continuación ambas metodologías, junto con las técnicas y herramientas utilizadas.

\textit{Scrum} es un marco de trabajo que posibilita un proceso ágil y liviano para administrar y controlar el
desarrollo de \textit{software}.
Es iterativo e incremental y cada iteración o \textit{sprint} concluye con una pieza de \textit{software} ejecutable que
incorpora nueva funcionalidad.

En este caso, se debió aplicar ciertas modificaciones, ya que el equipo de trabajo estaba conformado por un único
integrante (también conocido como \textit{Single Scrum}), destacando que el autor del proyecto combinó los roles de
Desarrollador, \textit{Product Owner} y \textit{Scrum Master}.

En principio se planearon seis \textit{sprints} con una duración de tres semanas cada una, de forma que la totalidad del
proyecto contempla una duración aproximada de cuatro meses.
Una vez definidas las historias de usuario se procedió a dividirlas en tareas más pequeñas que terminaron
formando parte del \textit{backlog} del proyecto.
Al finalizar cada \textit{sprint} se compartió el avance hasta el momento con la tutora del proyecto para obtener
la retroalimentación correspondiente.
Si bien su aplicación abarca la totalidad de las tareas del proyecto, se destaca para las tareas del
desarrollo de \textit{software} que fueron necesarias en este proyecto como la implementación del \textit{endpoint} que,
a partir del contenido de un correo electrónico, extraiga la información relevante y también la implementación de una
interfaz web simple que muestre el itinerario agregado de un usuario.

Por su parte, \textit{Crisp-DM} es una metodología para el desarrollo de proyectos de \textit{data mining} (DM)
independiente de las herramientas de \textit{software}.
Esta metodología estructura el trabajo en seis fases: Comprensión del negocio, Análisis de los datos, Preparación de los
Datos, Modelado, Evaluación y Despliegue.
Su aplicación permitió en este proyecto guiar la implementación de técnicas de extracción de información y la evaluación
de los resultados obtenidos.

\subsection{Técnicas y Herramientas}%
\label{subsec:tools}

En cuanto a las técnicas, en primer lugar se utilizaron tokenización y normalización, provenientes del área del
procesamiento de lenguaje natural (NLP).
También se utilizó Named Entity Recognition para poder extraer la información relevante de cada mail de reserva, con
la ayuda de expresiones regulares para aquellos casos que respeten patrones predecibles.

Respecto al desarrollo de la solución se utilizaron diversas herramientas de acuerdo con los requerimientos de cada
componente.
Para el proceso de extracción, transformación y carga (ETL) se utilizó principalmente el lenguaje de programación
Java, como así también Python.
Para la implementación de la aplicación web que muestra el itinerario consolidado del usuario se utilizó Spring Boot.

Por otro lado, para la organización del proyecto en general se utilizaron varias funcionalidades del sitio GitHub.com
destacando el versionado del código fuente con \textit{Git} y el manejo del \textit{backlog} con GitHub Projects.
Se detallan a continuación ambas herramientas:

\subsubsection{Git}

Git es una herramienta de control de versión ampliamente utilizada en la industria del \textit{software} que permite
llevar un registro de todos los cambios realizados en el tiempo.

Para este trabajo, se utilizó GitHub como plataforma~\cite{github}.
Allí se creó una organización denominada \texttt{email2trip}, que contiene todos los repositorios necesarios para el
proyecto.
En la \autoref{fig:extractor-git-remote} se puede ver el repositorio (\texttt{extractor}) que forma parte de
la organización y que actuó como servidor remoto del repositorio Git local.

En este proyecto su aplicación permitió revertir cambios cuando fue necesario, trabajar en distintas funcionalidades de
manera paralela mediante el uso de ramas o \textit{branches} y organizar la creación de los entregables de cada
\textit{sprint} mediante la funcionalidad de \textit{releases} y \textit{tags}.

También se creó un repositorio para el dataset (\texttt{dataset}) y uno para la redacción de este mismo reporte
(\texttt{report}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.00\textwidth]{images/extractor-git-remote}
    \figcaption{Repositorio \texttt{extractor} en GitHub.com}%
    \label{fig:extractor-git-remote}
\end{figure}

\FloatBarrier{}

GitHub Projects permite organizar el desarrollo mediante tableros Kanban que están poblados por cada una de las
\textit{issues} del proyecto.
Así, en una vista general del tablero o \textit{board layout} se pueden distinguir las \textit{issues} por estado: si
están todavía pendientes de análisis, en desarrollo, en etapa de pruebas o ya finalizada, dado que ya fueron integradas
a la implementación final.

En la \autoref{fig:github-projects-roadmap} se puede ver una vista del ciclo de vida de desarrollo o \textit{roadmap}
donde se puede apreciar la dimensión tiempo en cantidad de semanas y cómo las tareas se fueron resolviendo en cada
momento.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.00\textwidth]{images/github-projects-roadmap}
    \figcaption{Captura de ejemplo de una vista \textit{roadmap} del proyecto}{\sourcelink{GitHub Docs}{https://docs.github.com/es/issues/planning-and-tracking-with-projects/learning-about-projects/best-practices-for-projects}}%
    \label{fig:github-projects-roadmap}
\end{figure}

\subsection{Fuente de Datos}%
\label{subsec:data-source}

El \textit{dataset} cuenta con 31 mails distintos de aerolíneas que totalizan 41 tramos aéreos y 22 mails distintos de
compañías de trenes que suman 25 tramos.

\subsubsection{Correos de aerolíneas}

Los correos electrónicos de aerolíneas pueden ser agrupados en tres tipos: confirmación de reserva, recordatorio de
\textit{check-in} y ticket de embarque o \textit{boarding pass}.

Una confirmación de reserva es aquella que se obtiene luego de haber realizado la compra del pasaje aéreo.
De estos correos se espera extraer el código de la reserva, el nombre del pasajero y todo lo relevante para el tramo
o los tramos que la reserva implique.
Para cada tramo se espera extraer el código de aeropuerto de origen y destino, la fecha y hora de origen del tramo, el
código del vuelo y la aerolínea.

En la \autoref{fig:eml-yqb-ord-part-a} y \autoref{fig:eml-yqb-ord-part-b} se presenta un ejemplo que
corresponde a una reserva de la aerolínea Air Canada que incluye dos segmentos, uno desde Quebec City con destino a
Toronto y otro desde Toronto hacia la ciudad de Chicago, Estados Unidos.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-yqb-ord-1}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-yqb-ord-2}
    \end{minipage}
    \figcaption{Ejemplo de un correo electrónico de reserva de un vuelo de dos segmentos de Air Canada (primera parte).}%
    \label{fig:eml-yqb-ord-part-a}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-yqb-ord-3}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-yqb-ord-4}
    \end{minipage}
    \figcaption{Ejemplo de un correo electrónico de reserva de un vuelo de dos segmentos de Air Canada (segunda parte).}%
    \label{fig:eml-yqb-ord-part-b}
\end{figure}

\FloatBarrier{}

Los recordatorios de \textit{check-in} son aquellos correos electrónicos que el pasajero recibe cuando ya puede realizar
el \textit{check-in} para el primer tramo del vuelo reservado.
Los campos a extraer son los mismos que con el tipo anterior.

En la  \autoref{fig:eml-reminder-yqb-ord} se presenta un ejemplo que corresponde a un recordatorio de
la aerolínea American Airlines que incluye un segmento desde Quebec City con destino a la ciudad de Chicago,
Estados Unidos.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-reminder-yqb-ord-1}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-reminder-yqb-ord-2}
    \end{minipage}
    \figcaption{Ejemplo de un correo electrónico de recordatorio de \textit{check-in} de un vuelo de un tramo de American
    Airlines.}%
    \label{fig:eml-reminder-yqb-ord}
\end{figure}

\FloatBarrier{}

Los \textit{boarding pass} (ticket de embarque) corresponden a los correos electrónicos que el pasajero recibe una vez
realizado el \textit{check-in}.
Los campos a extraer también coinciden con los dos tipos anteriores, con la particularidad de que aquí siempre se
estará mencionando un único tramo.

En la \autoref{fig:eml-boardingpass-fra-vie} se presenta un ejemplo que corresponde a un \textit{boarding pass} de la
aerolínea Austrian del grupo Lufthansa que incluye un segmento desde Fráncfort (Alemania) con destino a la ciudad de
Viena (Austria).

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-boarding-pass-fra-vie-1}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-boarding-pass-fra-vie-2}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-boarding-pass-fra-vie-3}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/eml-boarding-pass-fra-vie-4}
    \end{minipage}
    \figcaption{Ejemplo de un correo electrónico de \textit{boarding pass} de un vuelo de un tramo de Austrian.}%
    \label{fig:eml-boardingpass-fra-vie}
\end{figure}

\FloatBarrier{}

\subsubsection{Correos de trenes}

Los correos de viajes en tren se agrupan también en tres tipos: confirmación de reserva, actualización de estado o
\textit{status update} y ticket de embarque o \textit{boarding pass}.

Se pueden apreciar en la \autoref{fig:train-booking} dos ejemplos de compañías distintas: la canadiense VIA Rail y
Deutsche Bahn (DB) de Alemania.
En la primera se puede ver un segmento que une las ciudades canadienses de Montreal y Ottawa.
En cuanto a la reserva de DB existen dos segmentos, uno de ida y otro de vuelta.
Ambos conectan la estación Wien Hauptbahnhof (Viena, Austria) con la estación München Hauptbahnhof (Múnich, Alemania).

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/train-eml-yul-yow}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/train-eml-vie-muc}
    \end{minipage}
    \figcaption{Ejemplo de correos electrónicos de reserva de tren de VIA Rail y Deutsche Bahn.}%
    \label{fig:train-booking}
\end{figure}

Los \textit{status update} corresponden a correos donde se indica que algún dato de la reserva ya realizada cambió,
por ejemplo si el tren se retrasó o si ya se le asignó una plataforma en la estación.
Si bien en esta implementación no se apunta a reconocer estos eventos particulares de cancelaciones o retrasos, los
mails siguen siendo útiles para la extracción, ya que en ellos se vuelve a indicar toda la información completa y
actualizada del viaje en tren en cuestión.
Continuando con la compañía DB se presenta a continuación un ejemplo de \textit{status update} en la
\autoref{fig:train-status-update} donde el viaje entre Viena y Munich ha sufrido un retraso y llegará minutos
más tarde de lo originalmente anunciado.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth]{images/train-eml-reminder-vie-muc}
    \figcaption{Ejemplo de un correo electrónico de actualización de estado de un viaje en tren de Deutsche Bahn.}%
    \label{fig:train-status-update}
\end{figure}

Por último se muestran dos ejemplos de \textit{boarding passes} en la \autoref{fig:train-boarding-pass}.
El primero corresponde a la compañía VIA Rail y conecta la ciudad de Montreal con la estación de tren Québec Gare du
Palais.
Cabe mencionar que, a diferencia de los \textit{boarding passes} de los vuelos, aquí es común encontrar en un mismo
correo los pases para dos o más tramos, por ejemplo, el de ida y el de vuelta.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/train-eml-boarding-pass-yul-yqb}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/train-eml-boarding-pass-vie-prg}
    \end{minipage}
    \figcaption{Ejemplo de \textit{boarding pass} de tren de VIA Rail y RegioJet.}%
    \label{fig:train-boarding-pass}
\end{figure}

El segundo \textit{boarding pass} proviene de RegioJet, una empresa que conecta varios países europeos.
Aquí se presenta un pase para viajar desde Wien Hbf hasta la estación Praha hlavní nádraží de República Checa (en el
correo se usa la abreviatura en inglés MS que corresponde a \textit{Main Station} o estación principal).

\subsection{Preparación de los Datos}%
\label{subsec:data-preparation}

En esta sección se menciona el proceso de anonimización realizado para
que en el \textit{dataset} no aparezcan datos sensibles de los pasajeros que voluntariamente se prestaron a compartir
sus correos electrónicos y el uso de una herramienta para llevar un control de versiones del \textit{dataset}.

\subsubsection{Anonimización}

Los mails originales, con los cuales se conforma el dataset, contienen datos sensibles de los pasajeros como su nombre y
apellido, el número de ticket del vuelo, el número de afiliado a un programa de millas, los últimos dígitos de la
tarjeta de crédito con la que se realizó la compra, entre otros.

El primer paso en la anonimización consiste en eliminar todos los archivos adjuntos que contenga el correo electrónico.
Es común que las aerolíneas incluyan un PDF con un resumen de todos los datos de la reserva del vuelo y otro PDF con los
detalles de facturación correspondientes.

Para ello, se implementó un \textit{script} de Python (\texttt{remove\_sensitive.py}) que se encarga de analizar todos
los \texttt{.eml}\footnote{Un archivo \texttt{.eml} es un formato estándar de correo electrónico que almacena el mensaje
completo, incluyendo encabezados, cuerpo y archivos adjuntos, en un único archivo.} presentes en el directorio
\texttt{private\_inbox/} y dejar los nuevos \texttt{.eml} sin los adjuntos en el directorio \texttt{raw/}.

Luego de aplicar este \textit{script}, se realiza una búsqueda y reemplazo manual de todos los datos sensibles que
aparecen en el contenido del nuevo \texttt{.eml}.
Los datos se reemplazan por valores ficticios predefinidos:

\begin{itemize}
    \item Nombre: Martin Miles Young
    \item Mail: martin.young@sample.com
    \item Código de Reserva (PNR): Cualquier alfanumérico (por ejemplo ABC1DE)
    \item Ticket Number: 1234567890123
\end{itemize}

\subsubsection{DVC}

Data Version Control (DVC) existe como una alternativa a Git pero pensada para el versionado de \textit{datasets},
modelos y archivos grandes, complementando a Git sin sobrecargarlo.

Para esta implementación se creó un repositorio \texttt{dataset} dentro de la organización email2trip en DagsHub.
Se puede acceder desde https://dagshub.com/email2trip/dataset/.

Así para cada archivo de extensión \texttt{.eml} existe uno de extensión \texttt{.dvc}.
Este repositorio DVC está sincronizado con el repositorio Git \texttt{dataset} de GitHub, donde ambos se pueden apreciar
en la \autoref{fig:dataset-dvc-git-remote}, de forma que cada vez que se desee agregar un nuevo correo electrónico se
debe cargar en el repositorio Git el archivo de extensión \texttt{.dvc} y el archivo del correo electrónico
\texttt{.eml} debe cargarse únicamente en el repositorio DVC\@.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dataset-dvc-remote}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dataset-git-remote}
    \end{minipage}
    \figcaption{Repositorio \texttt{dataset} en DagsHub y GitHub.com.}%
    \label{fig:dataset-dvc-git-remote}
\end{figure}

A continuación un ejemplo de los comandos necesarios para registrar en ambos repositorios un nuevo archivo que formará
parte del \texttt{dataset} (Ver \autoref{lst:dvc-bash-add}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={Comandos necesarios para versionar un nuevo archivo del \textit{dataset} usando Git y DVC.},
    label={lst:dvc-bash-add}
]
dvc add data/raw/flight_999.eml
git add data/raw/flight_999.eml.dvc
git commit -m "Added Email 999"
git push origin main
git push dagshub main
dvc push
\end{lstlisting}

\FloatBarrier{}

\subsubsection{Creación del \textit{dataset} estructurado}

Por último es necesario crear un archivo donde se relacione cada archivo \texttt{.eml} con los datos reales que se
esperan extraer del mismo.
Para ello se creó en el directorio \texttt{structured/} un archivo CSV (Ver \autoref{lst:dataset-csv}) donde se indica
el tipo de correo electrónico, todos los valores de los campos relevantes presentes en el correo, el nombre del archivo
.eml y el idioma del mismo.

\begin{lstlisting}[
    style=basecodestyle,
    caption={Ejemplo de las primeras líneas del archivo CSV del \textit{dataset}.},
    label={lst:dataset-csv}
]
id,type,booking,origin_airport,destination_airport,date,passenger_name,flight_code,depart_time,arrival_time,raw_filename,language
1,flight_confirmation,1ABCDE,LGA,YUL,2022-07-17,Martin Miles Young,AC8633,08:35,09:59,flight_001.eml,en
2,flight_confirmation,ABC1DE,ZRH,VIE,2026-01-28,Martin Miles Young,OS136,12:40,14:00,flight_002.eml,en
3,flight_confirmation,AB123C,VIE,CDG,2026-01-19,Martin Miles Young,OS361,07:10,09:15,flight_003.eml,en
4,flight_confirmation,AB123C,CDG,VIE,2026-01-22,Martin Miles Young,OS368,20:10,22:05,flight_003.eml,en
\end{lstlisting}

\subsection{Implementación del Modelo}%
\label{subsec:model-implementation}

En esta sección se detalla el proceso de implementación de la solución completa, incluyendo desde la lógica de
extracción de campos relevantes en un texto usando procesamiento de lenguaje natural (NLP) hasta el despliegue
de la aplicación web para que un usuario pueda registrarse, cargar sus
correos electrónicos y obtener un itinerario de viaje integral.
Como prueba de concepto, se realizó un trabajo de aplicación integral de todo el proceso, sobre los vuelos y viajes
en tren del itinerario.

\subsubsection{\textit{Pipeline end-to-end} de Procesamiento de Emails}

El \textit{pipeline} completo que inicia con un archivo de correo electrónico (de extensión \texttt{.eml}) y que
finaliza con la extracción de los datos relevantes se compone de los siguientes pasos:

\begin{itemize}
    \item Extracción de texto desde email
    \item Preprocesamiento con expresiones regulares
    \item Preprocesamiento con CoreNLP
    \item Extracción de información relevante
    \item Enriquecimiento de la información relevante
\end{itemize}

Para el caso de correos electrónicos correspondientes a vuelos se busca obtener el código de la reserva, el nombre del
pasajero y el detalle de uno o más segmentos de vuelo.
Cada segmento se compone de un código de vuelo, el aeropuerto de origen, el aeropuerto de destino y la fecha y hora de
inicio de vuelo.

En cuanto a los correos correspondientes a viajes en tren se espera extraer la misma información, donde cada segmento
tendrá un número de tren, la estación de origen, la estación de destino y la fecha y hora de inicio del viaje.

\paragraph{Extracción de texto desde email}

Retomando el ejemplo del correo electrónico de reserva (Ver \autoref{fig:eml-yqb-ord-part-a} y
\autoref{fig:eml-yqb-ord-part-b}), el primer paso consiste en obtener el contenido del correo electrónico y quedarse con
solo lo relevante.
Para ello se utiliza la librería~\cite{simple-java-mail} que convierte el contenido de
un \texttt{.eml} a formato HTML\@.
Para ello se utiliza el método \texttt{org.simplejavamail.api.email.Email\#getHTMLText}
y se consigue una instancia de \texttt{String} con todo el contenido HTML\@.
Retomando el mail de ejemplo, y considerando únicamente la sección donde se menciona el primer tramo del vuelo, desde
YQB a YYZ, el HTML obtenido es el siguiente (Ver \autoref{lst:flight026-first-segment-html}):

\begin{lstlisting}[
    style=basecodestyle,
    language=HTML,
    caption={Código HTML correspondiente al primer tramo del correo electrónico de Air Canada.},
    label={lst:flight026-first-segment-html}
]
<tr class="3D"ac-single-segment-info-container""><td valign="3D"top"" style="3D"padding-top: 30px; padding-bottom: = 30px; vertical-align: top; ""><table style="3D" font-size: 14px; font-family: Arial; color: #2A2A2A; width: 100%; border: 0px; border-collapse: collapse; "" cellpadding="3D"0"" cellspacing="3D"0"" border="3D"0"" width="3D"100%"" align="3D"= center""><tr><td width="3D"35%"" style="3D"width: 35%;""><p style="3D"font-size: 18px; margin-bottom: 10p= x;""><span style="3D"font-weight: bold""> Quebec City </span> YQB </p><p style="3D"font-size: 18px; margin-bottom: 10p= x;""> 10:50 </p><p class="3D"legal-info secondary-text"">Jean Lesage Int. Quebec </p><p></p></td><td width="3D"35%"" style="3D"width: 35%;""><p style="3D"font-size: 18px; margin-bottom: 10p= x;""><span style="3D"font-weight: bold""> Toronto </span> YYZ </p><p style="3D"font-size: 18px; margin-bottom: 10p= x;""> 12:25 </p><p class="3D"legal-info secondary-text"">Toronto-Pearson Int. , Terminal 1 </p><p></p></td><td width="3D"30%"" style="3D"width: 30%; padding-left= : 10px; border-left: 1px solid #BAA88D; font-size: 12px; line-height: 16px;= color: #4A4F55;""><p style="3D"font-size: 18px; margin-bottom: 5px= ;""><table style="3D" font-size: 14px; font-family: Arial; color: #2A2A2A; width: 100%; border: 0px; border-collapse: collapse; "" cellpadding="3D"0"" cellspacing="3D"0"" border="3D"0"" width="3D"100%"" align="3D"= center""><tr><td width="3D"15px"" style="3D"width: = 15px;""> =09=09=09=09<img style="3D" height: 15px; width: 15px; "" height="3D"15"" src="3D'https://www.aircanada.com/content/dam/aircanada/reve= nue/images/email-confirmation/airlines/ac.gif'"> </td><td style="3D"padding-left: 10px; fo= nt-size: 18px;""> AC1795 </td></tr></table></p><p class="3D"legal-info secondary-text"" style="3D="line-height: 16px; min-height: 16px; margin-bottom: 0;""> 1hr 35m </p> =09<p style="3D"line-height: 16px; min-height: 1= 6px; margin-bottom: 0;""> =09=09Business (I) =09</p><p class="3D"legal-info secondary-text"" style="3D="line-height: 16px; min-height: 16px; margin-bottom: 0;""> Operated by: AIR CANADA ROUGE <a href="3D"h= ttps://www.aircanada.com/ca/en/aco/home/fly/onboard/fleet.html#/!aircraft@a= 319"" target="3D"_blank"">A319-100</a> | <a href="3D"https://www.aircanada.com/= ca/en/aco/home/fly/onboard/wi-fi.html"" target="3D"_blank""><img src="3D"https:= //www.aircanada.com/content/dam/aircanada/revenue/images/availability/wifi.= png"" width="3D"12"" height="3D"12"" style="3D"width:12px; height:12px;MARGIN-RIG= HT: 5px"" alt="3D"Wi-Fi"">Wi-Fi</a></p></td></tr></table> =20 </td></tr>
\end{lstlisting}

Como se puede apreciar, además de nodos HTML con sus atributos y contenido, existen también fragmentos de texto como
``=09'' que corresponde a símbolos de \textit{quoted-printable}, una codificación del estándar MIME\@.

A continuación se utiliza la librería~\cite{jsoup} que consiste en un \textit{parser} HTML que
ofrece el método \texttt{org.jsoup.nodes.Element.text} para aplanar el HTML del mail y quedarse únicamente con el
texto visible, tal como si se estuviera visualizando el mail desde una aplicación de correo electrónico.

Continuando con la sección del mail de ejemplo, el texto obtenido es el siguiente (Ver \autoref{lst:flight026-first-segment-txt}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={Texto preprocesado correspondiente al primer tramo del correo electrónico de Air Canada.},
    label={lst:flight026-first-segment-txt}
]
Depart Sun 24 Jul, 2022 Business Class - Flexible Reward Quebec City YQB 10:50 Jean Lesage Int. Quebec Toronto YYZ 12:25 Toronto-Pearson Int. , Terminal 1 AC1795 1hr 35m Business (I) Operated by: AIR CANADA ROUGE A319-100 | Wi-Fi
\end{lstlisting}

Esta acción aplica también para todas las secciones posibles según el estándar~\cite{mime-wikipedia}.
Un correo electrónico cuenta con varias de estas secciones, algunas contienen adjuntos, otras contienen información de
reenvío del mail, y algunas incluso codifican el texto en~\cite{base64-wikipedia} de forma
que la biblioteca procede a decodificarlo.
Es importante destacar que el \textit{pipeline} trabaja únicamente con el texto visible de los mails, ignorando mucho
más contenido que podría ser relevante para identificar directamente áreas críticas del mail.
Atributos presentes en los \textit{tags} html, imágenes, adjuntos como archivos PDF y demás contenidos son ignorados
por el \textit{pipeline}.
Incluso el asunto del mail es ignorado.

Además, se aplica un preprocesamiento extra para eliminar \textit{footers} conocidos.
Por ejemplo aquellos nodos HTML identificados con cadenas como \texttt{.footer} son ignorados.
Durante la implementación este paso fue relevante para ignorar por ejemplo información de contacto de empresas de
aerolíneas que pueden confundir al algoritmo NLP (un caso es el nombre del presidente de la compañía, el cual se podría
confundir con el nombre del pasajero).
Dado que está comprobado que no existe información útil para el usuario ubicada en esta sección, entonces es seguro
eliminarla.

\paragraph{Preprocesamiento con expresiones regulares}

Otro preprocesamiento adicional que se aplica es la búsqueda de cadenas que puedan corresponder a códigos de vuelo,
mediante expresiones regulares (Ver \autoref{lst:normalize-flight-numbers}).
Un código de vuelo~\cite{iata-airline-codes} consiste en dos letras, seguido de un número
de entre 2 y 4 cifras.
Por ejemplo \texttt{AC8633} corresponde al vuelo 8633 de la aerolínea Air Canada.
En caso de que el mail contenga cadenas como \texttt{AC-8633} o \texttt{AC 8633} este preprocesamiento elimina el guion
o el espacio para que siempre se obtenga la cadena \texttt{AC8633}, facilitando así la tarea de la próxima etapa del
\textit{pipeline} para identificar códigos de vuelo usando una única convención.

\begin{lstlisting}[
    style=javacodestyle,
    caption={Expresiones regulares utilizadas para la normalización de códigos de vuelo.},
    label={lst:normalize-flight-numbers}
]
private static String normalizeFlightNumbers(String text) {
  Pattern FLIGHT_NUMBER_WITH_HYPHEN_SEPARATOR =
    Pattern.compile("\\b([A-Z][A-Z0-9]|[0-9][A-Z]|[A-Z][0-9])\\s*[-–]\\s*(\\d{2,4})\\b");
  Pattern FLIGHT_NUMBER_WITH_SPACE_SEPARATOR =
    Pattern.compile("\\b([A-Z][A-Z0-9]|[0-9][A-Z]|[A-Z][0-9])\\s+(\\d{2,4})\\b");
\end{lstlisting}

\paragraph{Preprocesamiento con CoreNLP}\label{par:corenlp}

Una vez que se cuenta con una instancia de \texttt{String} con únicamente el contenido a simple vista de un correo
electrónico se envía este texto al \textit{pipeline} de~\cite{stanford-corenlp}.
Stanford CoreNLP es una biblioteca de procesamiento de lenguaje natural desarrollada por Stanford que permite analizar
texto automáticamente, por ejemplo detectando tokens, lemas, categorías gramaticales, entidades y relaciones, entre
otros.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.00\textwidth]{images/pipeline}
    \figcaption{Diagrama del \textit{pipeline} de CoreNLP}{\sourcelink{CoreNLP}{https://stanfordnlp.github.io/CoreNLP/}}%
    \label{fig:corenlp-pipeline}
\end{figure}

Como se puede apreciar en la \autoref{fig:corenlp-pipeline}, el mismo cuenta con múltiples etapas que detallamos a
continuación:

\FloatBarrier{}

La tokenización consiste en convertir texto en tokens.
La forma más elemental resulta en dividir cada vez que aparece un espacio.
Por ejemplo para el texto \enquote{Thanks for flying.} la tokenización genera cuatro tokens:
\enquote{Thanks}, \enquote{for}, \enquote{flying} y \enquote{.}.
CoreNLP realiza la separación del texto en \textit{tokens} usando una colección de reglas ya previstas.
En el archivo de configuración se indica la propiedad \texttt{tokenize.language=en} para que utilice las reglas del
idioma inglés.
Desde este momento la implementación estará limitada exclusivamente a correos electrónicos de reservas redactados en
inglés.

La separación en oraciones (\textit{ssplit}, \textit{sentence splitting}) consiste en convertir un texto en oraciones.
Por ejemplo \enquote{Thanks for flying. Fly safe.}
será separado en las oraciones \enquote{Thanks for flying.} y \enquote{Fly safe.}
Aquí también CoreNLP realiza la separación en oraciones usando una colección de reglas ya previstas.

Luego, en la etapa de partes del discurso (\textit{pos}, \textit{parts of speech}) se asignan \textit{tags} o etiquetas
a los \textit{tokens} para marcarlos según su categoría gramatical.
Por ejemplo para el texto \enquote{Marie was born in Paris.} se etiqueta a \enquote{Marie} como \texttt{NNP}
(nombre propio singular), \enquote{was} como \texttt{VBD} (verbo pasado), \enquote{born} como \texttt{VBN}
(verbo participio pasado), \enquote{in} como \texttt{IN} (preposición), \enquote{París} nuevamente como \texttt{NNP} y
el punto final \enquote{.} como signo de puntuación.

En la \textit{lemmatization} se asocia una palabra con su lema o forma de diccionario.
Por ejemplo para la palabra ``was'' se mapea por la palabra ``be''.
Este paso lleva todo el contenido a un solo tiempo verbal para simplificar la tarea de las próximas etapas.

Finalmente, en la etapa \textit{Named Entity Recognition} (\textit{ner}), la más compleja de todo el pipeline de NLP,
se reconocen entidades en el texto como personas o nombres de empresas.
La librería utiliza uno o más modelos de secuencia de \textit{machine learning} para etiquetar las entidades.
Además permite la ejecución de reglas específicas para etiquetar, por ejemplo, fechas, tiempos y valores numéricos.

Para el idioma inglés la biblioteca reconoce por defecto 24 tipos distintos de entidades:
\begin{itemize}
    \item Nombres reconocidos: \texttt{PERSON}, \texttt{LOCATION}, \texttt{ORGANIZATION}, \texttt{MISC}
    \item Numéricos: \texttt{MONEY}, \texttt{NUMBER}, \texttt{ORDINAL}, \texttt{PERCENT}
    \item Temporales: \texttt{DATE}, \texttt{TIME}, \texttt{DURATION}, \texttt{SET}
    \item Otros: \texttt{EMAIL}, \texttt{URL}, \texttt{CITY}, \texttt{STATEORPROVINCE}, \texttt{COUNTRY},
    \texttt{NATIONALITY}, \texttt{RELIGION}, \texttt{TITLE}, \texttt{IDEOLOGY}, \texttt{CRIMINALCHARGE},
    \texttt{CAUSEOFDEATH}, \texttt{HANDLE}
\end{itemize}

A simple vista se puede apreciar que no existe una entidad aeropuerto o aerolínea por lo que es necesario crear reglas
adicionales para facilitar el reconocimiento de nuevos tipos de entidades.

Mostraremos a continuación las reglas introducidas para reconocer la entidad Aeropuerto.

Un aeropuerto de pasajeros se identifica por un código de tres letras denominado~\cite{iata-airport-code}.
A excepción de aeropuertos muy pequeños, cualquier aeropuerto cuenta con un identificador único de tres letras emitido
por IATA\@.

Para esta implementación se utilizó el \textit{dataset} público de~\cite{ourairports-data} que ofrece
la descarga de un archivo CSV con más de 84.000 registros que
representan a todos los aeropuertos del mundo.
Se procede a filtrar el mismo para considerar únicamente los aeropuertos que tengan un código IATA (descartando
aeropuertos pequeños).
Por ejemplo el aeropuerto ``Montreal / Pierre Elliott Trudeau International Airport'' se identifica con el código IATA
``YUL'' (Ver \autoref{lst:airports-csv-line}).

\begin{lstlisting}[
    style=basecodestyle,
    caption={Registro de ejemplo del \textit{dataset} Our Airports Data.},
    label={lst:airports-csv-line}
]
id,ident,type,name,latitude_deg,longitude_deg,elevation_ft,continent,iso_country,iso_region,municipality,scheduled_service,icao_code,iata_code,gps_code,local_code,home_link,wikipedia_link,keywords
1928,CYUL,large_airport,Montreal / Pierre Elliott Trudeau International Airport,45.467837,-73.742294,118,NA,CA,CA-QC,Montréal,yes,CYUL,YUL,CYUL,YUL,http://www.admtl.com/passager/Home.aspx,https://en.wikipedia.org/wiki/Montréal–Trudeau_International_Airport,"YMQ, Dorval Airport"
\end{lstlisting}

Para cada aeropuerto con código IATA se procede a crear un conjunto de reglas para que el \textit{pipeline} pueda
reconocer con éxito la entidad \texttt{AIRPORT\_CODE} mencionando todos los códigos de aeropuerto existentes.
Se creó un archivo \texttt{airport.rules} que asocia tanto el código IATA como la combinación de código IATA seguido del
nombre de la ciudad donde se ubica el aeropuerto.
Retomando el ejemplo de Montreal, el contenido del archivo de reglas es el siguiente
(Ver \autoref{lst:airport-rules-yul}):
\pagebreak

\begin{lstlisting}[
    style=basecodestyle,
    caption={Reglas de \texttt{airport.rules} para el aeropuerto YUL.},
    label={lst:airport-rules-yul}
]
YUL	AIRPORT_CODE	O,MISC,ORGANIZATION	2.0
YUL Montréal	AIRPORT_CODE	O,MISC,ORGANIZATION	2.0
\end{lstlisting}

La estructura de los archivos de reglas consiste en hasta 4 columnas separadas por un \textit{tab}.
En la primer columna se indica la cadena a corresponder, luego se indica  el tipo de categoría a asociar.
En la tercer columna se indican las categorías reconocidas que pueden ser reemplazadas (en el ejemplo estamos indicando
que si fue reconocido como \texttt{MISC} o como \texttt{ORGANIZATION} entonces es más importante \texttt{AIRPORT\_CODE}
y por lo tanto debería reconocerlo así).
Por último con el valor \texttt{2.0} estamos indicando la prioridad, que resulta útil para el desempate cuando dos
reglas aplican para una misma cadena.
De esta forma CoreNLP entiende que frente al tag ``YUL'' lo debe reconocer como un nuevo tipo \texttt{AIRPORT\_CODE}.

Debido a que CoreNLP puede reconocer a una cadena ``YUL Montréal'' como una única entidad, y anotarla como
\texttt{ORGANIZATION}, entonces necesitamos de una segunda regla para cada aeropuerto donde se ubica primero el código
IATA y luego el nombre de la ciudad correspondiente.

La particularidad de los nombres de los aeropuertos es que el modelo puede fácilmente confundirlo con el nombre de una
persona, por ejemplo del pasajero del vuelo.
Siguiendo con el mismo ejemplo, para la cadena ``Charles de Gaulle Airport'' el modelo lo anota como \texttt{PERSON}.
Es por eso que para cada aeropuerto se crean hasta tres reglas adicionales indicadas en el archivo
\texttt{airport-names.rules} donde para un nombre proveniente del dataset como ``Montreal / Pierre Elliott Trudeau
International Airport'' se incluye no sólo la cadena exacta, sino también la misma sin la palabra ``Airport'' y otra sin
la cadena ``International Airport''.
De esta forma, para el aeropuerto en cuestión, se obtienen las siguientes reglas
(Ver \autoref{lst:airport-names-rules-yul}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={Reglas de \texttt{airport-names.rules} para el aeropuerto YUL.},
    label={lst:airport-names-rules-yul}
]
Montreal / Pierre Elliott Trudeau	AIRPORT_NAME	O,MISC,ORGANIZATION,PERSON	2.0
Montreal / Pierre Elliott Trudeau International	AIRPORT_NAME	O,MISC,ORGANIZATION,PERSON	2.0
Montreal / Pierre Elliott Trudeau International Airport	AIRPORT_NAME	O,MISC,ORGANIZATION,PERSON	2.0
\end{lstlisting}

A diferencia del fragmento de reglas anterior aquí es necesario precisar que el tipo de entidad \texttt{PERSON} es
reemplazable por una nueva entidad \texttt{AIRPORT\_NAME}.
En principio para el procesamiento de los correos de vuelos no es necesario extraer el nombre del aeropuerto ya que los
identificadores IATA anotados como \texttt{AIRPORT\_CODE} son únicos.
De todas formas la identificación es útil para que el nombre del aeropuerto no sea identificado como \texttt{PERSON} y
así se facilita la tarea de identificar el nombre del pasajero de la reserva.

Por último se creó una regla de~\cite{stanford-tokensregex} donde se
rescata al aeropuerto cuando el texto visible queda con un formato como \enquote{EZE Buenos Aires Departing at} o
\enquote{EZE Buenos Aires Arriving at:}.
Para ello, en el archivo \nolinkurl{contextual-airport.tokensregex.rules} se definió lo siguiente
(Ver \autoref{lst:contextual-airport-tokensregex}):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Regla contextual para identificar el código del aeropuerto.},
    label={lst:contextual-airport-tokensregex}
]
ner = { type: "CLASS", value: "edu.stanford.nlp.ling.CoreAnnotations$NamedEntityTagAnnotation" }
{
  ruleType: "tokens",
  pattern: ((([{word:/[A-Z]{3}/}] [{word:/[A-Z][A-Za-z'.-]*/}] [{word:/[A-Z][A-Za-z'.-]*/}]?)) [{word:/Departing|Arriving/}] [{word:/at/}] [{word:/:/}]?),
  action: Annotate($2, ner, "AIRPORT_CODE"),
  result: "AIRPORT_CODE"
}
\end{lstlisting}

Como se puede apreciar se define un patrón donde primero aparece el código IATA de tres letras, seguido de 0 o más
palabras y luego las palabras exactas ``Departing'' o ``Arriving''.
Esta regla permite identificar al aeropuerto según el contexto en el que se ubica, pudiendo anotar únicamente el texto
correspondiente al aeropuerto como \texttt{AIRPORT\_CODE} y descartando el resto (en la parte de \texttt{action} se
indica \texttt{Annotate\string(\$2,\ldots\string)} para anotar el segundo grupo capturado, esto es,
\enquote{EZE Buenos Aires}).

A pesar de contar con reglas para la totalidad de códigos de aeropuertos existentes esto trae aparejado el problema de
los falsos positivos.
Una sigla en inglés como ``MAY'' que corresponde a un \textit{modal verb} será identificado incorrectamente como el
``Clarence A. Bain Airport'' en las Bahamas.
Del mismo modo la cadena ``MAX'' utilizada para indicar el máximo peso aceptado por la aerolínea en el equipaje ahora
será identificado como el ``Ouro Sogui Airport'' ubicado en Senegal.
Para estos casos extremos identificados, que siempre corresponden a aeropuertos pequeños, se decidió utilizar la
propiedad \texttt{ner.additional.regexner.commonWords} para indicarle a CoreNLP las palabras que deben ser tomadas como
palabras comunes del idioma inglés y así excluirlas de cualquier identificación.
Las palabras definidas en la implementación son las siguientes: ``API'', ``GET'', ``GST'', ``HST'', ``MAX'', ``MAY'', y
``VOL''.

Respecto a las entidades Códigos de Vuelo y Aerolínea, ya se explicó el preprocesamiento que se realiza antes de iniciar
el \textit{pipeline} de CoreNLP y que involucra a los códigos de vuelo definidos por IATA\@.
Ahora es momento de obtener el token ``AC8633'' y reconocerlo como una entidad de tipo \texttt{FLIGHT\_NUMBER}.
Para ello se utilizó el \textit{dataset} público de aerolíneas de~\cite{openflights-data}
que ofrece un CSV con más de 6.000 registros que representan a todas las aerolíneas del mundo.
Por ejemplo (Ver \autoref{lst:airlines-csv-line}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={Registro de ejemplo del \textit{dataset} OpenFlights.},
    label={lst:airlines-csv-line}
]
C1,C2,C3,C4,C5,C6,C7,C8
330,Air Canada,\N,AC,ACA,AIR CANADA,Canada,Y
\end{lstlisting}

Para cada aerolínea se cuenta con el código de dos letras de IATA (``AC''), el nombre y el país de origen de la misma,
entre otros.
Con esta información se puede crear un nuevo conjunto de reglas que modele el comportamiento de encontrar todos los
códigos de aerolínea (compuesto por dos caracteres alfanuméricos) seguidos de entre 2 y 4 dígitos que corresponden al
número de vuelo.
Para la aerolínea en cuestión el archivo de reglas es el siguiente (Ver \autoref{lst:airlines-rules-air-canada}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={Regla de \texttt{airlines.rules} para Air Canada.},
    label={lst:airlines-rules-air-canada}
]
AC\d{2,4}	FLIGHT_NUMBER	O,MISC	2.0
\end{lstlisting}

donde el nuevo tipo de entidad \texttt{FLIGHT\_NUMBER} tiene prioridad por sobre una entidad de tipo \texttt{MISC}.
Este conjunto se reglas se ubica en el archivo \texttt{airlines.rules}.

Utilizando el mismo \textit{dataset} se crea otro conjunto de reglas, ubicado en el archivo \texttt{airline-names.rules}
para definir el nuevo tipo de entidad \texttt{AIRLINE\_NAME} y que tenga prioridad por sobre tipos \texttt{MISC} y
\texttt{ORGANIZATION} (Ver \autoref{lst:airline-names-rules-air-canada}).

\begin{lstlisting}[
    style=basecodestyle,
    caption={Regla de \texttt{airline-names.rules} para Air Canada.},
    label={lst:airline-names-rules-air-canada}
]
AIR CANADA	AIRLINE_NAME	O,MISC,ORGANIZATION	2.0
\end{lstlisting}

Nuevamente, si bien no es necesario extraer el nombre de la aerolínea (ya que la primera porción del código de vuelo
es un identificador único), el hecho de anotar un nombre como nombre de aerolínea ayuda a que existan menos partes del
contenido sin anotar y que podrían ser candidatas a identificaciones incorrectas.

Detallaremos a continuación las reglas para definir la entidad Código de Reserva (PNR).

Aquí también IATA define el \texttt{PNR} o~\cite{passenger-name-record} a través de 6
caracteres alfanuméricos.
Para sumar dificultad de la extracción se recuerda que el código de vuelo (como ``AC8633'') ya está cumpliendo con el
formato de \texttt{PNR}.
Es por eso que aquí la estrategia para la extracción es distinta y consiste en identificar el \texttt{PNR} por contexto.
Se establecen un conjunto de reglas \texttt{TokensRegex} para extraer el \texttt{PNR} cuando aparece luego de frases como
``Confirmation Number'', ``Booking Code'', ``Booking Reference'', entre otras.
En el archivo \texttt{pnr.tokensregex.rules} se indican seis reglas.
El principio del archivo es el siguiente (Ver \autoref{lst:pnr-tokensregex-rules}):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Una de las reglas para identificar el código de reserva del vuelo.},
    label={lst:pnr-tokensregex-rules}
]
ner = { type: "CLASS", value: "edu.stanford.nlp.ling.CoreAnnotations$NamedEntityTagAnnotation" }
$PNR_CODE = "/[A-Z0-9]{6}/"
$SEPARATOR = "/[:-]/"
{
  ruleType: "tokens",
  pattern: ([{word:/[Bb]ooking/}] [{word:/[Rr]eference/}] [{word:$SEPARATOR}]? ([{word:$PNR_CODE}])),
  action: Annotate($1, ner, "PNR_CODE"),
  result: "PNR_CODE"
}
\end{lstlisting}

Por su parte, en un mismo mail de confirmación de reserva pueden aparecer múltiples fechas y horas correspondientes por
ejemplo al despegue o aterrizaje de cualquiera de los tramos de la reserva.
Además es común que se indique la fecha en que se realizó la confirmación o incluso la fecha límite hasta donde se
pueden realizar modificaciones a la reserva sin costo adicional.
Es por eso que en esta etapa de anotación sólo resulta útil anotar todos los campos correspondientes a fecha y hora como
\texttt{TIME} y la extracción con contexto se realizará en la siguiente etapa.
Solo así podremos asociar por ejemplo la fecha y hora de despegue al primer tramo de una reserva y no al segundo.

Las reglas por defecto fueron suficientes para la mayoría de los correos electrónicos, ya que CoreNLP provee la
biblioteca~\cite{stanford-sutime} que permite reconocer por ejemplo fechas con
múltiples formatos como \texttt{31{-}12{-}2026}, \texttt{31/12/2026} o
\texttt{2026{-}31{-}12}.

Sólo fue necesaria una regla \texttt{TokensRegex} extra (Ver \autoref{lst:sutime-boarding-pass-rule}):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Regla para identificar fechas en \textit{boarding passes}.},
    label={lst:sutime-boarding-pass-rule}
]
ner = { type: "CLASS", value: "edu.stanford.nlp.ling.CoreAnnotations$NamedEntityTagAnnotation" }
{
  ruleType: "tokens",
  pattern: (([{word:/\d{2}[A-Z]{3}\d{2}/}])),
  action: Annotate($1, ner, "DATE"),
  result: "DATE"
}
\end{lstlisting}

para que en mails correspondientes a \textit{boarding passes}, una cadena como ``22SEP25'' sea interpretada como
``22/09/2025''.

Por último se define un archivo \texttt{segment-context.rules} con las siguientes dos reglas
(Ver \autoref{lst:segment-context-rules})
:
\begin{lstlisting}[
    style=basecodestyle,
    caption={Dos reglas para identificar los marcadores de tramos.},
    label={lst:segment-context-rules}
]
Departing	FLIGHT_SEGMENT_MARKER	O,MISC,ORGANIZATION	2.0
Arriving	FLIGHT_SEGMENT_MARKER	O,MISC,ORGANIZATION	2.0
\end{lstlisting}

Si bien en la siguiente etapa se podría buscar la palabra exacta ``Departing'' es mejor anotarla como una nueva entidad
de tipo \texttt{FLIGHT\_SEGMENT\_MARKER} para buscar múltiples entidades de un tipo en particular y así identificar uno
o más tramos de vuelos dentro de una misma reserva.

Retomando el \autoref{lst:flight026-first-segment-txt} de la etapa inicial de extracción desde el mail, en la
\autoref{fig:tcol-flights} se muestran las entidades identificadas al final de esta etapa de preprocesamiento con CoreNLP\@.
\pagebreak
\begin{tcolorbox}[
    colback=black!2,
    colframe=black!35,
    title={Entidades reconocidas en un fragmento de texto de una reserva aérea},
    breakable
]

    \medskip
    \noindent

    {\raggedright%
    Depart \entitydate{Sun 24 Jul, 2022} Business Class {-} Flexible Reward \entitystateorprovince{Quebec} City
    \entityairportcode{YQB} \entitytime{10:50} \\
    \entityperson{Jean Lesage Int.} \entityorganization{Quebec Toronto} \entityairportcode{YYZ} \\
    \entitytime{12:25} Toronto-Pearson Int., Terminal \entitynumber{1} \entitypnrcode{AC1795}\\
    \entityduration{1hr} \entitynumber{35m} Business (I) Operated by: \entityairlinename{AIR CANADA} ROUGE A319{-}100 | Wi-Fi\par
    }

    \medskip
    \noindent

\end{tcolorbox}
\noindent\begin{minipage}{\linewidth}
             \captionsetup{type=figure,hypcap=false}
             \caption{Entidades reconocidas.}%
             \label{fig:tcol-flights}
\end{minipage}

Si bien la salida es la esperada para las entidades de tipo \texttt{DATE}, \texttt{TIME}, \texttt{AIRPORT\_CODE} y
\texttt{PNR\_CODE}, también se obtienen identificaciones incorrectas.
Por ejemplo el caso de \enquote{Jean Lesage Int.} que es incorrectamente anotado como \texttt{PERSON} cuando en realidad
es el nombre completo del aeropuerto en cuestión.
Esto demuestra la necesidad de una siguiente etapa donde, para un conjunto de entidades de tipo \texttt{PERSON} (entre
ellas la esperada, la incorrecta del ejemplo y cualquier otra que pueda aparecer con el procesamiento del resto del mail)
se deberá aplicar cierto criterio para quedarse sólo con una.

Las entidades resaltadas en color negro corresponden a la implementación por defecto de CoreNLP\@.
Allí también se aprecian etiquetados incorrectos que en principio no afectan la tarea de extracción planteada.
Por ejemplo, asociar ``Quebec Toronto'' a \texttt{ORGANIZATION} es un error porque corresponden a dos ciudades distintas
ubicadas en diferentes provincias.

\subparagraph{Preprocesamiento con CoreNLP para trenes}%
\label{subpar:extraction-trains}

El reconocimiento de información en el ambiente de trenes plantea ciertos desafíos cuando se lo compara con el de
vuelos.
El hecho de que no exista una organización como IATA que estandarice la información hace no sólo que existan diferencias
respecto a los vuelos sino que plantea variantes entre redes de trenes distintas.

Para esta implementación se contemplan dos redes de trenes: la canadiense y la europea.
A simple vista ya existen diferencias de formato y longitud en los campos relevantes a extraer.

Tal como se aprecia en la \autoref{fig:train-booking} el tren canadiense es el tren 33 (indicado como Train 33) mientras
que en Europa, donde es habitual que varias compañías de trenes distintas operen sus servicios en una misma estación,
el servicio de DB se identifica como RJX 260, acercándose al concepto de código de vuelo ya mencionado anteriormente.
Se plantean entonces un conjunto de reglas para poder extraer el identificador de tren, contemplando que puede ser sólo
un número, como así también una cadena alfanumérica que puede incluir un espacio.
Al mismo tiempo se descarta la extracción de la empresa de tren operadora del viaje debido a las diferencias planteadas.

Además del identificador del tren, otro campo que plantea matices dependiendo la empresa o la red de trenes es el código
de reserva.
En la misma figura se ve que para la empresa canadiense el código de reserva es un alfanumérico de seis caracteres
(coincidente con el concepto de PNR establecido por la IATA) mientras que para el sistema europeo el concepto de un
identificador de la reserva no es relevante y en el correo se indica un \textit{order number} que consiste en un número
de doce cifras.
Por lo tanto se plantean reglas para poder extraer ambos tipos de código de reserva.

La detección de segmentos de tren sí plantea similitudes con la de vuelos ya que conceptualmente coinciden.
En lugar de un aeropuerto de origen y destino se deben buscar las estaciones de tren de origen y destino.
La fecha y hora de salida y el nombre del pasajero son campos que no agregan diferencias a la lógica de extracción.
Sí corresponde detenerse en la identificación de estaciones de tren ya que se necesita recurrir a
\textit{datasets} distintos.

Otro efecto de que no exista una organización que regule todos los viajes en tren del mundo hace que sea más difícil
conseguir un \textit{dataset} que contenga, por ejemplo, todas las estaciones de tren del mundo.
En busca de estandarizar la fuente de información, independientemente del país o sistema de trenes, se optó por el
formato GTFS\footnote{\textit{General Transit Feed Specification}} que consiste en un formato de datos estandarizado
para que los operadores de transporte puedan definir los detalles de sus servicios (estaciones, rutas, horarios, etc.).
Además del GTFS estático existe un GTFS en tiempo real que ofrece, por ejemplo, la ubicación de los trenes en el mapa.

Del portal MobilityDatabase~\cite{mobilitydatabase} se descargan los archivos GTFS del sistema
canadiense, donde el único operador es VIA Rail y cuenta con 168 estaciones de tren de Canadá y Estados Unidos.

Para el sistema europeo también se recurre al mismo portal para descargar los archivos GTFS de 14 operadores
(entre ellos DB de Alemania, ÖBB de Austria y SBB de Suiza).
El \textit{dataset} cubre 420 estaciones de tren de 17 países europeos.

De los archivos obligatorios que el estándar GTFS requiere sólo se utiliza el archivo \texttt{stops.txt} donde cada
registro corresponde a una estación.
La información es limitada, ya que si bien se incluye el nombre de la estación y sus coordenadas, no se incluye la
ciudad ni el país donde se ubica la estación.

Un desafío que se circunscribe al sistema de trenes europeo es la detección del nombre de una estación de tren en
distintos idiomas y con distintos sufijos.
Si bien la implementación actual está limitada a correos electrónicos en inglés, las empresas de tren suelen incluir
no solo el nombre original de la estación sino también una variante en inglés o en el idioma de la compañía de tren.

Es por eso que es necesario recurrir a un \textit{dataset} adicional proveniente de la empresa
~\cite{trainlineeu} que provee mucha más información para cada estación de tren.
En primer lugar indica el identificador que coincide con la especificación GTFS\@.
Esto permite agregar información adicional directamente a cada estación del \textit{dataset} original.
Los campos que se destacan son los de internacionalización, como las columnas \texttt{info:de}, \texttt{info:en} y
\texttt{info:cs} donde para cada estación se indica el nombre en alemán, inglés y checo respectivamente.
Por ejemplo para la estación austríaca Wien Hbf, en \texttt{info:en} se obtiene Vienna y en \texttt{info:cs} Vídeň.
Para el caso de la estación Praha hl.n., en \texttt{info:de} se obtiene Prag y en \texttt{info:en} se obtiene Prague.

Por último es necesario contemplar un tratamiento particular para los sufijos que existen en los nombres de las
estaciones, donde aquí también varía según el idioma.
En el idioma checo el sufijo de la estación principal es la abreviatura \texttt{hl.n.}
Entonces para la estación \texttt{Praha hl.n.} es necesario reconocer también las cadenas \texttt{Praha, hl.n.},
\texttt{Prague}, \texttt{Prague Main Station}, \texttt{Prague, MS}, \texttt{Prague Central Station}, entre otras.

A continuación se plantea en el \autoref{lst:train-rules-prg} parte del contenido del archivo de reglas para
identificar a la estación principal de Praga:

\begin{lstlisting}[
    style=basecodestyle,
    caption={Reglas de \texttt{eu-train-station.rules} para la estación principal de Praga.},
    label={lst:train-rules-prg}
]
Prague, MS	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	3.0
Prague, hl\.n\.	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	3.0
Prague Main Station	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	3.0
Prague hl\.n\.	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	3.0
Prague Central Station	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	3.0
Praha hl.n.	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	2.0
Praha, hl\.n\.	TRAIN_STATION	O,PERSON,MISC,ORGANIZATION,LOCATION,CITY,STATE_OR_PROVINCE,COUNTRY	3.0
\end{lstlisting}

Respecto a las entidades reconocidas en un correo de trenes se retoma el ejemplo de la \autoref{fig:train-booking} para
la reserva con tramos entre Wien Hbf y München Hbf.
En la \autoref{fig:tcol-trains} se muestran las entidades reconocidas:
\pagebreak
\begin{tcolorbox}[
    colback=black!2,
    colframe=black!35,
    title={Entidades reconocidas en un fragmento de texto de una reserva de tren},
    breakable
]

    \medskip
    \noindent

    {\raggedright%
    Hello Mr \entityperson{Martin Young}.
    The order with the order number \entitytrainticketcode{123456789012} was booked on \entitytime{13.12.2025 at 18:19}
    as follows:
    Sitzplatzreservierung, 2.\ \entityperson{Klasse}, \entitytrainnumber{RJX 260}
    from \entitytrainstation{Wien Hbf}, \entitytime{21.12.2025 06:28}
    to \entitytrainstation{München Hbf Gl.5{-}10}, \entitytime{21.12.2025 10:33}\par
    }

    \medskip
    \noindent

\end{tcolorbox}
\noindent\begin{minipage}{\linewidth}
    \captionsetup{type=figure,hypcap=false}
    \caption{Entidades reconocidas.}%
    \label{fig:tcol-trains}
\end{minipage}

\paragraph{Extracción de Información Relevante}

La salida del \textit{pipeline} de CoreNLP implementado en \nolinkurl{xyz.email2trip.pipeline.FlightPipeline} consiste en
una instancia de \nolinkurl{edu.stanford.nlp.pipeline.CoreDocument} que cuenta con un conjunto de entidades anotadas.
A continuación se indica cómo se realiza la extracción de los campos relevantes a partir de la búsqueda de entidades de
un tipo en particular.

Para el código de reserva (PNR) se busca una entidad de tipo \texttt{PNR\_CODE}.
En caso de que exista más de una se obtiene la que apareció primero.
En cuanto al nombre del pasajero primero se buscan las entidades de tipo \texttt{PERSON}.
Como aquí es común encontrar más de una se deben ponderar todas las entidades bajo un criterio determinístico y así
elegir la que tiene una mejor puntuación, esto es, la que es más probable que represente el nombre del pasajero.
En las pruebas, y retomando el ejemplo de la palabra ``Max'' la misma era etiquetada como \texttt{PERSON} con una
\textit{confidence} mayor que, por ejemplo, el nombre verdadero del pasajero del correo (``Martin Miles Young'').
CoreNLP ofrece el método \texttt{edu.stanford.nlp.pipeline.CoreEntityMention\#entityTypeConfidences} que permite obtener
el valor de \textit{confidence} de una entidad candidata para el tipo deseado.
Por ejemplo una entidad puede tener un valor de \textit{confidence} para \texttt{PERSON} de 0.9 y al mismo tiempo uno de
0.75 para el tipo \texttt{ORGANIZATION}.
Es por eso que el proceso para conseguir el nombre del pasajero consiste en obtener los valores de \textit{confidence}
de todas las entidades para el tipo \texttt{PERSON} y quedarse con el de mayor valor.
El criterio anterior no fue necesario y se necesitó realizar un filtro extra para darle mayor prioridad a entidades que
más se parecieran a nombres completos.
Contando la cantidad de palabras de una entidad se prioriza aquella que tenga al menos dos palabras, para corresponder
con el patrón de nombre y apellido.
Así una cadena como ``Max'' no luce como un nombre y apellido pero ``Martin Miles Young'' sí.

La extracción de los datos del vuelo requiere cierta lógica adicional dado que se espera extraer más de un campo del
mismo tipo de entidad.
Por ejemplo para una reserva con un solo vuelo se necesita conocer el aeropuerto de origen o despegue y el aeropuerto de
destino o aterrizaje.
Allí ya se necesitan dos entidades de tipo \texttt{AIRPORT\_CODE} donde, por la naturalidad del idioma inglés, la primer
entidad corresponderá al aeropuerto de origen y la segunda al de destino.
También se busca una entidad de tipo \texttt{FLIGHT\_NUMBER} para el código del vuelo en cuestión.
Respecto a la fecha y hora de origen del vuelo, se necesita información de contexto ya que la primer entidad de tipo
\texttt{DATE} que aparezca en el correo podría representar información distinta.
Es necesario que la entidad de tipo \texttt{DATE} esté cerca del código del aeropuerto destino.
Lo mismo aplica para la hora de despegue.
La lógica mencionada para aeropuerto origen, destino, fecha y hora de origen y código de vuelo puede encapsularse en lo
que se denomina la identificación de un tramo de vuelo.
En un único mail de reserva de un vuelo se pueden encontrar uno o más segmentos o tramos.
Por lo tanto se procederá a identificar tramos donde cada uno se compone de dos entidades de tipo
\texttt{AIRPORT\_CODE}, un \texttt{DATE} y un \texttt{TIME} para el origen y un \texttt{FLIGHT\_NUMBER}.
Para ello se implementaron cuatro \textit{layouts} posibles, es decir, cuatro formas en que podrían aparecer estas
entidades, contemplando así la totalidad de los correos de reservas presentes en el \textit{dataset}.

\begin{itemize}
    \item Opción 1: El patrón clásico que utiliza la mayoría de las aerolíneas
    \begin{itemize}
        \item Regla:
        \texttt{AIRPORT \ldots\ TIME \ldots\ AIRPORT\_CODE}\\
        \texttt{\ldots\ TIME \ldots\ FLIGHT\_NUMBER}
        \item Ejemplo: ``LGA 08:35 YUL 09:59 AC8633''
    \end{itemize}
    \item Opción 2: Otra forma bastante utilizada donde primero aparece el código del vuelo
    \begin{itemize}
        \item Regla:
        \texttt{FLIGHT\_NUMBER \ldots\ AIRPORT\_CODE \ldots\ TIME}\\
        \texttt{\ldots\ AIRPORT\_CODE \ldots\ TIME}
        \item Ejemplo: ``AC132 YVR Vancouver Departing at 13:00 YYZ Toronto Arriving at 20:28''
    \end{itemize}
    \item Opción 3: Un \textit{layout} partido donde las fechas y horas aparecen al final
    \begin{itemize}
        \item Regla:
        \texttt{AIRPORT\_CODE \ldots\ AIRPORT\_CODE \ldots\ FLIGHT\_NUMBER}\\
        \texttt{\ldots\ DATE TIME \ldots\ DATE TIME}
        \item Ejemplo: ``EZE ATL DL110 Departs 07/20/2020 8:20 PM Arrives 07/21/2020 5:48 AM''
    \end{itemize}
    \item Opción 4: El más utilizado por los correos de tipo \textit{boarding pass} donde primero aparece la fecha del embarque
    \begin{itemize}
        \item Regla:
        \texttt{DATE \ldots\ FLIGHT\_NUMBER \ldots\ AIRPORT\_CODE \ldots\ TIME}\\
        \texttt{\ldots\ AIRPORT\_CODE \ldots\ TIME}
        \item Ejemplo: ``22SEP25 OS208 FRA 12:50 VIE 14:15''
    \end{itemize}
\end{itemize}

En todas ellas la notación \texttt{\ldots} acepta cero o más entidades de cualquier tipo distinto a los buscados.
El último paso es evitar conservar dos segmentos que resultan equivalentes, esto es, que tienen los mismos datos.
Esto es común en confirmaciones de vuelos donde la información aparece primero con los datos del pasajero y luego
aparece por segunda vez sólo para indicar la política de equipaje para cada segmento.
Una vez que se extrajeron los campos relevantes de una reserva se envía esa información al proceso de enriquecimiento o
\textit{enrichment} para conseguir más información a partir de los identificadores únicos que resultan de algunas de las
entidades identificadas.

Retomando el correo electrónico de ejemplo de dos segmentos, luego de la extracción se obtiene la siguiente salida en
formato JSON que se muestra en el \autoref{lst:output-extractor-core-json}:

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Salida del \textit{pipeline} de extracción en formato JSON.},
    label={lst:output-extractor-core-json}
]
{
  "pnr": "1ABCDE",
  "passenger": "Martin Miles Young",
  "segments": [
    {
      "flightNumber": "AC1795",
      "departureAirport": "YQB",
      "arrivalAirport": "YYZ",
      "departureDate": "2022-07-24",
      "departureTime": "10:50"
    },
    {
      "flightNumber": "AC8909",
      "departureAirport": "YYZ",
      "arrivalAirport": "ORD",
      "departureDate": "2022-07-24",
      "departureTime": "14:55"
    }
  ]
}
\end{lstlisting}

Respecto a los segmentos de tren y retomando el correo electrónico de dos segmentos, se muestra a continuación en el
\autoref{lst:output-extractor-core-json-train} la salida correspondiente en formato JSON\@.

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Salida del \textit{pipeline} de extracción en formato JSON.},
    label={lst:output-extractor-core-json-train}
]
{
  "bookingCode": "123456789012",
  "passenger": "Martin Young",
  "segments": [
    {
      "trainNumber": "RJX 260",
      "originStation": "Wien Hbf",
      "destinationStation": "München Hbf Gl.5-10",
      "departureDate": "2025-12-21",
      "departureTime": "06:28"
    },
    {
      "trainNumber": "IC 406",
      "originStation": "München Hbf Gl.5-10",
      "destinationStation": "Wien Hbf",
      "departureDate": "2025-12-21",
      "departureTime": "18:35"
    }
  ]
}
\end{lstlisting}

\paragraph{Enriquecimiento de la Información Relevante}

Con los mismos \textit{datasets} de vuelos y aerolíneas utilizados para la creación de reglas de CoreNLP se puede enriquecer el
JSON obtenido.

Para los aeropuertos, a partir del código IATA, se pueden obtener las coordenadas geográficas para poder ubicarlo más
adelante en un mapa.
También el nombre completo del aeropuerto, la provincia y el país donde está ubicado.
El \textit{dataset} incluye además, para los aeropuertos más importantes, \textit{links} útiles que redirigen al sitio
web del aeropuerto o a la página de Wikipedia correspondiente.

En cuanto a las aerolíneas, utilizando las dos primeras letras del código de vuelo se puede obtener no sólo el nombre
completo de la aerolínea sino también el país de origen de la misma.

Para la implementación se cargan en memoria ambos \textit{datasets} ya que entre ambos no alcanzan los 90.000 registros.
Así, a partir del resultado final de la etapa de extracción, se consigue un JSON enriquecido como el que se presenta a
continuación (Ver \autoref{lst:output-extractor-enrich-json}, en la salida se ocultan algunos campos repetidos):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Salida del módulo de enriquecimiento en formato JSON.},
    label={lst:output-extractor-enrich-json}
]
{
  "extraction": {
    "extraction": { ...
    },
    "enrichment": {
      "segments": [
        {
          "flightNumber": "AC1795",
          "departureDate": "2022-07-24",
          "departureTime": "10:50",
          "departureAirport": {
            "code": "YQB",
            "name": "Quebec Jean Lesage International Airport",
            "city": "Quebec",
            "countryCode": "CA",
            "countryName": "Canada",
            "latitude": 46.7911,
            "longitude": -71.393303,
            "homeLink": "http://www.aeroportdequebec.com/",
            "wikipediaLink": "https://en.wikipedia.org/wiki/Qu%C3%A9bec/Jean_Lesage_International_Airport"
          },
          "arrivalAirport": {
            "code": "YYZ",
            "name": "Toronto Pearson International Airport",
            "city": "Toronto",
            "countryCode": "CA",
            "countryName": "Canada",
            "latitude": 43.675935,
            "longitude": -79.629421,
            "homeLink": "http://www.torontopearson.com/",
            "wikipediaLink": "https://en.wikipedia.org/wiki/Toronto_Pearson_International_Airport"
          },
          "airline": {
            "code": "AC",
            "name": "Air Canada",
            "alias": null,
            "country": "Canada",
            "countryCode": "CA"
          }
        },
        {
          "flightNumber": "AC8909",
          "departureDate": "2022-07-24",
          "departureTime": "14:55",
          "departureAirport": { ...
          },
          "arrivalAirport": {
            "code": "ORD",
            "name": "Chicago O'Hare International Airport",
            "city": "Chicago",
            "countryCode": "US",
            "countryName": "United States",
            "latitude": 41.9786,
            "longitude": -87.9048,
            "homeLink": "https://www.flychicago.com/ohare/home/pages/default.aspx",
            "wikipediaLink": "https://en.wikipedia.org/wiki/O'Hare_International_Airport"
          },
          "airline": { ...
          }
        }
      ]
    }
  }
}
\end{lstlisting}

Respecto a los correos de trenes, además de cargar en memoria los archivos de GTFS ya mencionados se necesita una tarea
de enriquecimiento adicional.
Dado que sólo se cuenta con el nombre de la estación y sus coordenadas geográficas, se utiliza un servicio web externo
para la geolocalización inversa.
Este proceso consiste en invocar al servicio web con los valores de latitud y longitud y así obtener el nombre de la
ciudad y el nombre del país correspondientes a esas coordenadas.

La API de~\cite{nominatim-api-overview} ofrece un uso gratuito con
la limitación de procesar hasta una coordenada por segundo.
Para eliminar esta restricción se puede utilizar un proveedor pago o el despliegue de una instancia propia.

Para cada geocodificación se realiza un \textit{request} HTTP a la API REST de Nominatim usando el path
\texttt{/reverse} donde se utilizan los parámetros \textit{lat} y \textit{lon}, entre otros.
En el \textit{request} se indica además que el formato de respuesta debe ser \texttt{jsonv2} y que la cantidad de
resultados es una.
Así la respuesta consiste en un JSON con el nodo \texttt{address} que cuenta con tres campos relevantes: \texttt{city},
\texttt{country} y \texttt{country\_code}.

Un ejemplo de respuesta para la geocodificación de la coordenada ``(48.184923, 16.375865)'' es el
siguiente (Ver \autoref{lst:api-nominatim-response-json-reverse}):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Ejemplo de respuesta de la API de Nominatim.},
    label={lst:api-nominatim-response-json-reverse}
]
{
  "place_id": 186114760,
  "licence": "Data © OpenStreetMap contributors, ODbL 1.0. http://osm.org/copyright",
  "osm_type": "relation",
  "osm_id": 109166,
  "lat": "48.2083537",
  "lon": "16.3725042",
  "category": "boundary",
  "type": "administrative",
  "place_rank": 7,
  "importance": 0.8308606398092985,
  "addresstype": "city",
  "name": "Vienna",
  "display_name": "Vienna, Austria",
  "address": {
    "city": "Vienna",
    "ISO3166-2-lvl4": "AT-9",
    "country": "Austria",
    "country_code": "at"
  },
  "boundingbox": [
    "48.1179069",
    "48.3226679",
    "16.1818310",
    "16.5775132"
  ]
}
\end{lstlisting}

A continuación se muestra un ejemplo del resultado final de la etapa de enriquecimiento (Ver
\autoref{lst:output-extractor-enrich-json-train}) retomando el ejemplo de trenes ya mencionado (en la salida se ocultan
algunos campos):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Salida del módulo de enriquecimiento en formato JSON.},
    label={lst:output-extractor-enrich-json-train}
]
{
  "extraction": {
    "trainExtraction": { ...
    },
    "trainEnrichment": {
      "segments": [
        {
          "trainNumber": "RJX 260",
          "originStation": "Wien Hbf",
          "originLatitude": 48.184923,
          "originLongitude": 16.375865,
          "originCity": "Vienna",
          "originCountryCode": "AT",
          "originCountryName": "Austria",
          "destinationStation": "München Hbf Gl.5-10",
          "destinationLatitude": 48.140094,
          "destinationLongitude": 11.555373,
          "destinationCity": "Munich",
          "destinationCountryCode": "DE",
          "destinationCountryName": "Germany",
          "departureDate": "2025-12-21",
          "departureTime": "06:28"
        },
        {
          "trainNumber": "IC 406",
          "originStation": "München Hbf Gl.5-10",
          "originLatitude": 48.140094,
          "originLongitude": 11.555373,
          "originCity": "Munich",
          "originCountryCode": "DE",
          "originCountryName": "Germany",
          "destinationStation": "Wien Hbf",
          "destinationLatitude": 48.184923,
          "destinationLongitude": 16.375865,
          "destinationCity": "Vienna",
          "destinationCountryCode": "AT",
          "destinationCountryName": "Austria",
          "departureDate": "2025-12-21",
          "departureTime": "18:35"
        }
      ]
    }
  }
}
\end{lstlisting}

\paragraph{Recomendación de Puntos de Interés}%
\label{par:poi}

Esta sección cubre la funcionalidad adicional de ofrecer recomendaciones de puntos de interés que el usuario podría
visitar en cada una de las ciudades destino de la reserva (ciudades según los aeropuertos destino o estaciones de
tren de llegada de cada segmento).

En lugar de utilizar una base de datos de POIs (\textit{points of interest}) se optó por utilizar un
\textit{Large Language Model} (LLM) de forma local al que se le solicitan recomendaciones de POIs para un destino y se
exige que la respuesta del modelo sea estructurada en formato JSON\@.

Las recomendaciones sobre lugares a visitar dependen de las preferencias o intereses de cada persona.
Algunas pueden preferir lugares más relacionados a la naturaleza mientras que a otros les interesen más las compras y
elijan visitar tiendas o mercados.
Es por eso que se definió una forma de categorizar a los usuarios en función de los intereses de una forma simple.
Se definen cinco categorías de intereses:

\begin{itemize}
    \item \texttt{CULTURE\_HISTORIC}: Museos o centros históricos
    \item \texttt{NATURE}: Parques Nacionales, plazas, lagos
    \item \texttt{SHOPPING}: Tiendas y mercados
    \item \texttt{NIGHTLIFE}: Vida nocturna
    \item \texttt{RELIGIOUS}: Iglesias y conventos.
\end{itemize}

El usuario al momento de solicitar una recomendación puede elegir hasta dos de estos intereses.
El proceso de recomendación deberá priorizar los puntos de interés que se ajustan a los intereses del usuario, sin
impedir que pudieran recomendarse también algunos puntos de interés más relacionados a intereses no seleccionados.

Para esta implementación se decidió utilizar Gemma 4 E4B como LLM\@.
Es un modelo recientemente lanzado que pertenece a la familia de modelos abiertos desarrollados por Google DeepMind.
Ofrece compatibilidad con más de 140 idiomas y la versión E4B cuenta con 4.500 millones de parámetros totales efectivos.
De acuerdo a lo enunciado en la tarjeta del modelo, Gemma 4 se recomienda para tareas como la generación de texto, la
programación y el razonamiento.
La variedad de modelos (en función de la cantidad de parámetros) permite que estos puedan ser implementados en entornos
que van desde teléfonos de alta gama hasta \textit{laptops} y servidores, es decir, sin necesidad de contar con una \textit{GPU}
potente.

El modelo se ejecuta en Ollama, una herramienta que permite ejecutar modelos localmente, tanto en CPU como en GPU,
exponiendo una \textit{API HTTP} para interactuar con el modelo y así facilitar la integración en aplicaciones.
En la implementación se utiliza Ollama dentro de un contenedor de Docker por lo que el modelo se ejecuta sólo en CPU\@.
Sin embargo el uso de Ollama permite que, al momento de realizar el \textit{deploy} de la aplicación en algún proveedor
\textit{cloud}, se puede definir una propiedad y así dejar de utilizar Ollama local y usar una API de pago.

Para interactuar con el LLM desde código Java se utiliza el \textit{framework} Spring AI\@.
Esta biblioteca se encarga de pedirle al modelo que la salida cumpla con un \textit{schema} para lograr la salida
esperada en formato JSON\@.
Además permite el reemplazo de un modelo LLM por otro, incluso de distintos fabricantes, sin necesidad de realizar
cambios en la implementación.

El \textit{prompt} cuenta con tres secciones, el \textit{prompt} de sistema (\textit{system prompt}), el \textit{prompt}
del usuario y el \textit{prompt} para que la respuesta tenga la estructura deseada.

El \textit{prompt} de sistema es fijo para todas las recomendaciones de todos los usuarios.
En la implementación se utilizó lo siguiente (Ver \autoref{lst:system-prompt-txt}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={\textit{System prompt} para que el LLM se comporte como un asistente de viajes.},
    label={lst:system-prompt-txt}
]
You are a travel itinerary assistant.
Keep descriptions short and practical.
Prefer famous landmarks, neighborhoods, museums, parks, markets, and viewpoints.
Use canonical place names that can be geocoded later.
\end{lstlisting}

Como se puede apreciar se establecen instrucciones generales para guiar al modelo en el estilo de respuesta, todavía
sin mencionar la estructura.

El \textit{prompt} del usuario contendrá la instrucción concreta y será parametrizado con los intereses del usuario,
la ciudad deseada y la cantidad de días elegidos.
Para todas las recomendaciones se decidió fijar el valor fijo de tres puntos de interés por día.

Un ejemplo de \textit{prompt} de usuario para armar un itinerario de 3 días en la ciudad de Montreal, Canadá es el
siguiente (Ver \autoref{lst:user-prompt-txt}):

\begin{lstlisting}[
    style=basecodestyle,
    caption={\textit{User prompt} para que el LLM responda con recomendaciones para armar un itinerario concreto.},
    label={lst:user-prompt-txt}
]
Build a sightseeing itinerary for Montreal, Canada for 3 days.
User interests: Culture & Historic, Nature
Requirements:
- Return exactly 3 days in the itinerary.
- Each day must include:
  - day: the day number starting at 1
  - title: a short title
  - summary: a one-sentence summary
  - places: exactly 3 places to visit for that day
- Each place must include:
  - name
  - description: one concise sentence
  - category: one of CULTURE_HISTORIC, NATURE, SHOPPING, NIGHTLIFE, RELIGIOUS, NONE
- Avoid temporary events or unstable recommendations.
- The city field must be "Montreal".
- The country field must be "Canada".
- The days field must be 3.
- Every single day must have exactly 3 places.
- Favor places aligned with the user interests when possible.
- If the user interests are not provided or do not clearly apply, fall back to the city's general highlights.
- You only know these allowed category values: CULTURE_HISTORIC, NATURE, SHOPPING, NIGHTLIFE, RELIGIOUS, NONE
- Do not use any other category values beyond CULTURE_HISTORIC, NATURE, SHOPPING, NIGHTLIFE, RELIGIOUS, NONE.
- Set category to the best-fitting general category for the POI, even if it is not one of the user's preferred interests.
- If a POI does not clearly fit any category, set category to NONE.
\end{lstlisting}

En primer lugar se indica la instrucción concreta indicando la ciudad, el país de la ciudad y la cantidad de días.
A continuación se enumeran los intereses del usuario (en este caso \texttt{CULTURE\_HISTORIC} y \texttt{NATURE}).

Luego se menciona una serie de requerimientos y recomendaciones que se fueron obteniendo luego de varias pruebas
interactuando con el modelo.
Como se puede apreciar en el \textit{prompt} se pide organizar las recomendaciones por día, donde para cada día se
indica el número de día, un título y una breve descripción.
Cada día contará con exactamente tres puntos de interés.
Cada punto de interés debe contar con el nombre, una oración que lo describa y si se ajusta a una de las cinco
categorías establecidas.

A continuación todas las demás oraciones son instrucciones para que el modelo no cometa errores.
Si bien varias de ellas parecen redundantes, se fueron agregando luego de varias pruebas, siempre con la intención de
reforzar la instrucción y obtener el resultado esperado en el primer intento.
Allí se puede ver la instrucción \enquote{Favor places aligned with the user interests when possible.} para que priorice los
puntos de interés que el usuario espera.

Por último, Spring AI completa el \textit{prompt} agregando una tercera sección luego del \textit{system prompt} y el
\textit{user prompt}.
Aquí el \textit{framework} le exige al modelo que la respuesta debe ser estructurada y le provee el \textit{schema}
JSON correspondiente.

Retomando el ejemplo de recomendaciones de Montreal, la tercera sección consiste en lo siguiente
(Ver \autoref{lst:spring-ai-prompt-txt}):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={\textit{Prompt} creado por SpringAI para que el LLM se ajuste a la salida estructurada esperada.},
    label={lst:spring-ai-prompt-txt}
]
Your response should be in JSON format.
Do not include any explanations, only provide a RFC8259 compliant JSON response following this format without deviation.
Do not include markdown code blocks in your response.
Remove the ```json markdown from the output.
Here is the JSON Schema instance your output must adhere to:
`{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "type" : "object",
  "properties" : {
    "itinerary" : {
      "type" : "array",
      "items" : {
        "type" : "object",
        "properties" : {
          "day" : {
            "type" : "integer"
          },
          "places" : {
            "type" : "array",
            "items" : {
              "type" : "object",
              "properties" : {
                "category" : {
                  "type" : "string"
                },
                "description" : {
                  "type" : "string"
                },
                "name" : {
                  "type" : "string"
                }
              },
              "required" : [ "category", "description", "name" ],
              "additionalProperties" : false
            }
          },
          "summary" : {
            "type" : "string"
          },
          "title" : {
            "type" : "string"
          }
        },
        "required" : [ "day", "places", "summary", "title" ],
        "additionalProperties" : false
      }
    }
  },
  "required" : [ "itinerary" ],
  "additionalProperties" : false
}```
\end{lstlisting}

Al igual que el \textit{system prompt} esta sección es fija e invariante para todas las recomendaciones.
Se puede ver cómo se indican los tipos de datos esperados (por ejemplo que el día debe ser un número pero el nombre debe
ser un \texttt{String}), se explicita que los campos son obligatorios y se mencionan los nombres que deben tener las
propiedades del JSON\@.

A modo de ejemplo, si se desea obtener recomendaciones de 3 días para la ciudad de Toronto priorizando los intereses
\texttt{CULTURE\_HISTORIC} y \texttt{NATURE} se obtiene un JSON como el que se puede ver en el
\autoref{lst:output-core-recommender-json}, (en la salida se ocultan algunos campos)

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Salida del recomendador en formato JSON.},
    label={lst:output-core-recommender-json}
]
{
  "city": "Toronto",
  "country": "Canada",
  "days": 3,
  "itinerary": [
    {
      "day": 1,
      "title": "Downtown History and Waterfront Views",
      "summary": "Explore Toronto's core history and enjoy scenic views along the waterfront.",
      "places": [
        {
          "name": "Royal Ontario Museum",
          "description": "A major museum featuring world cultures and natural history exhibits.",
          "category": "CULTURE_HISTORIC"
        },
        {
          "name": "Nathan Phillips Square",
          "description": "A prominent public square featuring the iconic Toronto City Hall.",
          "category": "CULTURE_HISTORIC"
        },
        {
          "name": "Harbourfront Centre",
          "description": "An area along the lake offering views and public spaces for recreation.",
          "category": "NATURE"
        }
      ]
    },
    {
      "day": 2,
      "title": "Cultural Immersion and Green Spaces",
      "summary": "Dive into diverse neighborhoods and relax in Toronto's beautiful green spaces.",
      "places": [
        { ... },
        { ... },
        { ... }
      ]
    },
    { ... }
  ]
}
\end{lstlisting}

\paragraph{Geocodificación de los Puntos de Interés}

Para poder mostrar las recomendaciones obtenidas en un mapa es necesario contar con las coordenadas geográficas
(latitud y longitud) de cada punto de interés.

Inicialmente se le solicitaba al LLM para que conteste también con las coordenadas de cada punto recomendado
pero los resultados resultaron siempre incorrectos.
Un LLM no es una fuente confiable de datos geoespaciales precisos ya que las respuestas que genera son siempre
probabilísticas y puede ``alucinar'' coordenadas inexistentes.
Por ejemplo en las pruebas para la ciudad de Toronto, todas las coordenadas que respondía el LLM estaban
desplazadas varios kilómetros y no sirven para lo que se espera en esta implementación.

Es por eso que se decidió utilizar un servicio de geocodificación para que, a partir del nombre del punto de interés, la
ciudad y el país se obtengan las coordenadas geográficas precisas de ese punto.
La API de~\cite{nominatim-api-overview} ofrece un uso gratuito con
la limitación de geolocalizar una cadena de texto por segundo.
En la implementación se tienen en cuenta estas restricciones por lo que el proceso de localizar todos los puntos de
interés de un itinerario puede demorar varios segundos.
Para eliminar esta restricción se puede utilizar un proveedor pago o el despliegue de una instancia propia.

Para cada geocodificación se realiza un \textit{request} HTTP a la API REST de Nominatim usando el path
\texttt{/search} donde el texto de consulta o \textit{query} es siempre el nombre del punto de interés, seguido del
nombre de la ciudad y luego del nombre del país, separado por coma.
Por ejemplo para el punto de interés ``CN Tower'' de la ciudad de Toronto en Canadá la \textit{query} es la cadena
``CN Tower, Toronto, Canada''.
En el \textit{request} se indica además que el formato de respuesta debe ser \texttt{jsonv2} y que la cantidad de
resultados es una.
Así la respuesta consiste en un JSON con tres campos relevantes: \texttt{lat}, \texttt{lon} y \texttt{display\_name}
(que corresponde al nombre completo y en esta implementación no es utilizado).

Un ejemplo de respuesta para la geocodificación del punto de interés ``Royal Ontario Museum, Toronto, Canada'' es el
siguiente (Ver \autoref{lst:api-nominatim-response-json}):

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Ejemplo de respuesta de la API de Nominatim.},
    label={lst:api-nominatim-response-json}
]
{
  "lat": 43.6676565,
  "lon": -79.3946988,
  "display_name": "Royal Ontario Museum, 100, Queen's Park, University, University-Rosedale, Toronto, Golden Horseshoe, Ontario, M5S 2C6, Canada"
}
\end{lstlisting}

Para algunas recomendaciones del LLM, dado que no coincide con el nombre exacto que el servicio de
geocodificación espera, no se podrán conseguir las coordenadas geográficas.
En la implementación se decidió utilizar las coordenadas del aeropuerto o de la estación de tren para marcar al menos la
zona de la ciudad donde el punto de interés se encuentra.

A modo de ejemplo, si se desea obtener recomendaciones de 3 días para la ciudad de Toronto priorizando los intereses
\texttt{CULTURE\_HISTORIC} y \texttt{NATURE} se obtiene un JSON como el que se muestra en el
\autoref{lst:output-extractor-server-json}, (en la salida se ocultan algunos campos)

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Resultado final luego de la extracción, enriquecimiento, recomendación y geolocalización, en formato JSON.},
    label={lst:output-extractor-server-json}
]
{
  "city": "Toronto",
  "country": "Canada",
  "days": 3,
  "itinerary": [
    {
      "day": 1,
      "title": "Downtown History and Waterfront Views",
      "summary": "Explore Toronto's core history and enjoy scenic views along the waterfront.",
      "places": [
        {
          "name": "Royal Ontario Museum",
          "description": "A major museum featuring world cultures and natural history exhibits.",
          "category": "CULTURE_HISTORIC",
          "latitude": 43.6676565,
          "longitude": -79.3946988
        },
        {
          "name": "Nathan Phillips Square",
          "description": "A prominent public square featuring the iconic Toronto City Hall.",
          "category": "CULTURE_HISTORIC",
          "latitude": 43.6527083,
          "longitude": -79.3838423
        },
        {
          "name": "Harbourfront Centre",
          "description": "An area along the lake offering views and public spaces for recreation.",
          "category": "NATURE",
          "latitude": 43.6397544,
          "longitude": -79.381371
        }
      ]
    },
    { ... },
    { ... }
  ]
}
\end{lstlisting}

En este punto ya se cuenta con toda la información necesaria para mostrarle al cliente las recomendaciones de los puntos
de interés a visitar y las ubicaciones de cada uno de ellos en un mapa.

\subsection{Arquitectura}%
\label{subsec:architecture}

Para la implementación de la aplicación web se decidió utilizar la siguiente arquitectura:

\begin{itemize}
    \item Aplicación Web (WebApp)
    \begin{itemize}
        \item Vanilla JS para el \textit{front-end} de la aplicación web
        \item Spring Boot para la lógica del servidor \textit{back-end}, ofreciendo los endpoints de extracción y
        recomendaciones
    \end{itemize}
    \item Base de Datos (DB): Una base relacional para conseguir la persistencia requerida por el \textit{back-end}
    \item Ollama: Herramienta que sirve el LLM de las recomendaciones que invoca el \textit{back-end}
    \item APIs externas
    \begin{itemize}
        \item Nominatim: Para la geolocalización de puntos de interés y la geolocalización inversa de las estaciones
de tren, ambas invocadas por el \textit{back-end}
        \item MediaWiki: Para la obtención de una imagen representativa del viaje (visible en ``My Trips''), invocada
        por el \textit{front-end}
    \end{itemize}
\end{itemize}

En la \autoref{fig:architecture-diagram} se detalla el diagrama de arquitectura correspondiente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.64\textwidth]{docs/diagram}
    \figcaption{Diagrama de arquitectura.}%
    \label{fig:architecture-diagram}
\end{figure}

\FloatBarrier{}

\subsubsection{Persistencia: Base de Datos}

Se requiere una base de datos relacional para poder almacenar los datos del usuario y cada uno de sus viajes, incluyendo
los resultados de todas las extracciones y los puntos de interés de todas las recomendaciones generadas.
Se decidió utilizar PostgreSQL porque es la base de datos relacional más popular.

Respecto al manejo de usuarios registrados en la aplicación se modela la tabla \texttt{user\_account} donde, para cada
usuario, se almacena la dirección de correo electrónico (que actúa al mismo tiempo como nombre de usuario) y un
\textit{hash} de la contraseña del usuario.

Cada usuario cuenta con uno o más viajes, modelados por la tabla \texttt{trip}.
Esta tabla cuenta con el nombre y el tipo del viaje, el nombre del pasajero, el código de reserva, las fechas de inicio y fin
del viaje y los nombres de la ciudad y país destino del viaje.

Cabe mencionar que, para facilitar la implementación, existen campos en las tablas de la base que podrían ser obtenidos
con una invocación al servicio de \textit{enrichment}.
Sin embargo se opta por la persistencia de esta información para reducir las invocaciones al servicio indicado.
Además se facilita la implementación dado que la estructura de la tabla coincide con el archivo ``JSON'' que se obtiene
a la salida del \textit{pipeline} de extracción.

Cada viaje cuenta con uno o más segmentos, modelados por la tabla \texttt{trip\_segment}.
Esta tabla contiene los datos comunes a vuelos y trenes, como el número identificador, la fecha y hora de salida.
Para los vuelos, la información relacionada a aeropuertos y a la aerolínea se almacena en la tabla
\texttt{trip\_flight\_segment\_details}.
Por su parte, para los trenes, la información relacionada a estaciones se almacenan en la tabla
\texttt{trip\_train\_segment\_details}.

Por último es necesario almacenar las recomendaciones de POIs.
Para cada recomendación se persiste una tupla en la tabla \texttt{trip\_saved\_recommendation} donde se indica la
ciudad, el país y la cantidad de días elegida.
Para cada día de una recomendación existe un registro en la tabla \texttt{trip\_saved\_recommendation\_day}, incluyendo
dos campos de texto con título y el resumen de ese día.
Finalmente, para cada punto de interés de un día de una recomendación, la tabla
\texttt{trip\_saved\_recommendation\_place} lista el nombre, la descripción del POI, la categoría (según los cinco
intereses posibles ya mencionados) y las coordenadas geográficas (latitud y longitud) del mismo.

En la \autoref{fig:db-model-diagram} se detalla el modelo relacional correspondiente.

\begin{figure}[H]
    \centering
    \resizebox{0.96\textwidth}{!}{\input{images/db-model-diagram.tikz}}
    \figcaption{Modelo relacional de la base de datos.}%
    \label{fig:db-model-diagram}
\end{figure}

\FloatBarrier{}

\paragraph{Flyway}

Para el modelado de la base de datos se utilizó Flyway, una herramienta que permite definir scripts SQL incrementales,
de forma que, al iniciar la aplicación, se ejecutan automáticamente los scripts que falten.
Esto facilita el agregado de nuevas tablas y campos y permite el versionado del \textit{schema} de la base de datos.

Por ejemplo, el primer \textit{script}, llamado \nolinkurl{V1\_\_create\_user\_account.sql} contiene únicamente la
definición de la tabla que modela a los usuarios (Ver \autoref{lst:flyway-1}) ya que es lo único que se necesitaba
para realizar una primera implementación de la funcionalidad de registro y login.

\begin{lstlisting}[
    style=sqlcodestyle,
    caption={Script de Flyway con la creación de la tabla de usuarios.},
    label={lst:flyway-1}
]
CREATE TABLE user_account (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    email VARCHAR(255) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    CONSTRAINT uk_user_account_email UNIQUE (email)
);
\end{lstlisting}

En la etapa final de la implementación se necesitó agregar los campos latitud y longitud para los POIs de las
recomendaciones.
Para ello se creó el script \nolinkurl{V5\_\_add\_recommendation\_place\_coordinates.sql} (Ver \autoref{lst:flyway-5})
con la sentencia \texttt{ALTER TABLE} correspondiente.

\begin{lstlisting}[
    style=sqlcodestyle,
    caption={Script de Flyway con el agregado de coordenadas de POIs},
    label={lst:flyway-5}
]
ALTER TABLE trip_saved_recommendation_place
    ADD COLUMN latitude DOUBLE PRECISION,
    ADD COLUMN longitude DOUBLE PRECISION;
\end{lstlisting}

\subsubsection{Back-End: Spring Boot}

Para la implementación del \textit{back-end} se decidió utilizar Spring Boot porque facilita la implementación de
aplicaciones web escritas en el lenguaje Java.
Spring Boot se destaca por reducir la configuración necesaria para conseguir una aplicación web desplegada gracias a
dependencias por defecto, convenciones establecidas y la autoconfiguración.
Permite integrar de forma sencilla varios componentes necesarios como APIs REST, acceso a base de datos, seguridad,
entre otras.

El \textit{back-end} se separó en seis módulos distintos, utilizando la herramienta Maven para organizar proyectos Java.
A continuación el detalle de los módulos de \textit{back-end}, donde los tres primeros ya fueron explicados en detalle
en secciones anteriores:

\begin{itemize}
    \item \texttt{extractor-core}: Contiene todo lo relacionado al \textit{pipeline} de extracción de Stanford NLP
    \item \texttt{extractor-enricher}: Enriquecimiento de la información
    \item \texttt{extractor-recommender}: Recomendación de puntos de interés
    \item \texttt{extractor-server}: Implementación del \textit{back-end} que invoca los tres módulos anteriores,
    administra el acceso de los usuarios y logra la persistencia en la base de datos.
    \item \texttt{extractor-api}: Conjunto de clases comunes al proyecto (definiciones compartidas por más de un módulo)
    \item \texttt{extractor-common}: Métodos utilitarios utilizados por más de un módulo (como la lectura de un CSV)
\end{itemize}

\paragraph{Spring Data}

Se utilizó Spring Data para simplificar la comunicación entre el \textit{back-end} y la base de datos.
Permite abstraer gran parte del código fuente necesario para la interacción con la base mediante la definición de
repositorios con interfaces, de forma que el \textit{framework} genera automáticamente el comportamiento necesario.

Por ejemplo, se definió un \texttt{TripRepository} (Ver \autoref{lst:spring-data-trip-repository}) con sólo dos
operaciones, de forma que Spring Data genera automáticamente las \textit{queries} a la base, sólo analizando el nombre
que se le dio a los métodos (que siguen una convención ya definida por la herramienta).

\begin{lstlisting}[
    style=javacodestyle,
    caption={Repositorio de Viajes usando Spring Data},
    label={lst:spring-data-trip-repository}
]
public interface TripRepository extends JpaRepository<Trip, Long> {

    List<Trip> findAllByUserEmailOrderByIdDesc(String email);

    Optional<Trip> findByIdAndUserEmail(Long id, String email);

}
\end{lstlisting}

Así no se necesitan implementar manualmente \textit{queries} de \texttt{SELECT} para conseguir todos los viajes de
un usuario, basta con invocar a \texttt{findAllByUserEmailOrderByIdDesc} dado que se indica que el tipo de dato de
retorno es una lista de viajes (\texttt{List<Trip>}) y sólo se necesita el identificador del usuario (el email).

\paragraph{Spring Security}

Otra herramienta de la familia Spring es Spring Security, que apunta a gestionar de una forma sencilla la seguridad
de las aplicaciones en cuanto a la autenticación y autorización.
Su función principal es controlar quién puede acceder al sistema y qué acciones puede realizar el usuario una vez
logueado.

Para la implementación basta con definir un método donde se indiquen los \textit{endpoints} que requieren autenticación
y cuáles no (Ver \autoref{lst:spring-security-filter-chain}). Por ejemplo se indica que todos pueden acceder a
\texttt{/login} y \texttt{/register} de forma que todo lo que no está mencionado se restringe automáticamente (como
por ejemplo el endpoint de \texttt{core-enricher}).

\begin{lstlisting}[
    style=javacodestyle,
    caption={Lógica de autenticación usando Spring Security},
    label={lst:spring-security-filter-chain}
]
@Bean
public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
    http
            .authorizeHttpRequests(authorize -> authorize
                    .requestMatchers("/", "/login", "/register", "/error").permitAll()
                    .requestMatchers("/api/auth/register").permitAll()
                    .requestMatchers("/assets/**").permitAll()
                    .anyRequest().authenticated()
            )
            .csrf(CsrfConfigurer::spa)
            .formLogin(form -> form
                    .loginPage("/login")
                    ...
                    .permitAll()
            )
            ...
}
\end{lstlisting}

Como se ve en el fragmento de código, se utilizó una protección contra solicitudes maliciosas basada en tokens
Cross-Site Request Forgery (CSRF) que consisten en valores únicos generados por el servidor y verificados en cada
solicitud que modifique el estado de la aplicación.
De esta forma, en caso de que un atacante envíe una petición maliciosa, al no tener el \textit{token} esperado la operación será
rechazada.

\subsubsection{Front-End: Vanilla JS}

La interfaz de usuario requerida para la implementación cuenta con solo cuatro vistas: registro, login, listado de
viajes y detalle de un viaje.
Dada la simpleza del requerimiento se decidió utilizar JavaScript nativo (Vanilla JS) para conseguir una aplicación
web, en principio pensada para \textit{desktop} pero con funcionalidad \textit{responsive} para poder ser utilizada
también desde dispositivos móviles con pantallas más pequeñas.

En el directorio \texttt{assets/} del módulo \texttt{extractor-client} se ubican los archivos JavaScript y CSS
necesarios.
Además se utilizó Bootstrap, un framework front-end de componentes y reglas CSS que permite construir aplicaciones web
\textit{responsive} consistentes a partir de estilos y componentes ya definidos.

Los archivos de extensión HTML se encuentran ubicados en el directorio \texttt{templates/} del módulo
\texttt{extractor-server}.
Esto es así porque se decidió utilizar Thymeleaf, un motor de plantillas para Java que permite generar HTML de forma
dinámica, insertando así datos provenientes del servidor dentro de plantillas HTML de forma sencilla que luego se
renderizan en la página.

Por último, y como ya se mencionó en \autoref{subsec:architecture} desde el \textit{front-end} se invoca a la API
de MediaWiki para conseguir una imagen de la ciudad que corresponde al destino final del viaje.
Esta imagen se puede ver en la sección ``My Trips''.
La API es gratuita y para esta implementación se realiza un \texttt{request} GET (Ver \autoref{lst:js-mediawiki})
probando primero con la cadena \texttt{Ciudad, País}.
En caso de no obtener un resultado se intenta luego con las cadenas \texttt{Ciudad (País)} y \texttt{Ciudad}.
La respuesta de la API es un JSON que contiene la URL directa del recurso buscado, esto es, la imagen representativa
de una ciudad.
Para reducir las invocaciones a la API se implementa una caché a nivel de la sesión del lado del navegador.
\pagebreak
\begin{lstlisting}[
    style=javacodestyle,
    caption={Uso de la API de MediaWiki para imagen de una ciudad},
    label={lst:js-mediawiki}
]
const fetchImageUrlForTitle = async (title) => {
    const url = new URL("https://en.wikipedia.org/w/api.php");
    url.searchParams.set("action", "query");
    url.searchParams.set("format", "json");
    url.searchParams.set("origin", "*");
    url.searchParams.set("redirects", "1");
    url.searchParams.set("prop", "pageimages");
    url.searchParams.set("piprop", "thumbnail");
    url.searchParams.set("pithumbsize", "640");
    url.searchParams.set("titles", title);

    const response = await fetch(url);
};
\end{lstlisting}

\subsection{Despliegue}%
\label{subsec:deployment}

En esta sección se detalla el uso de Docker para orquestar los servicios necesarios, Terraform para definir la
infraestructura en la nube necesaria y las particularidades del proveedor de servicios en la nube elegido, Azure.

\subsubsection{Despliegue Local: Docker}

Para facilitar el despliegue de la aplicación se utiliza Docker.
Para ello se define un archivo Dockerfile (Ver \autoref{lst:docker-dockerfile}) en la raíz del proyecto
\texttt{extractor-parent} que se encarga de crear
la imagen con todos los módulos del proyecto en dos etapas, lo que se conoce como \textit{multi-stage build}.
En la primera etapa se compila la aplicación por completo, a partir de la imagen base \texttt{eclipse-temurin:25-jdk}.
En lugar de ejecutar la aplicación sobre la misma imagen utilizada para la compilación, se provee solo el \texttt{.jar}
resultante a una imagen más liviana, a partir de la imagen base \texttt{eclipse-temurin:26-jre}, reduciendo así el
tamaño final del contenedor.
Por último se inicia la aplicación mediante el comando \texttt{java -jar app.jar}.

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Archivo Dockerfile (se omiten varias líneas (...) correspondientes a la etapa \texttt{build})},
    label={lst:docker-dockerfile}
]
FROM eclipse-temurin:26-jdk AS build

WORKDIR /workspace

(...)

RUN --mount=type=cache,target=/root/.m2 \
    ./mvnw -pl server -am package -Dmaven.test.skip=true

(...)

FROM eclipse-temurin:26-jre

WORKDIR /app

COPY --from=build /workspace/server/target/extractor-server-1.0-SNAPSHOT.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "/app/app.jar"]

\end{lstlisting}

En el archivo de Docker Compose \texttt{compose.yaml} (Ver \autoref{lst:docker-compose}), también ubicado en la raíz del
proyecto, se define la orquestación completa del entorno de ejecución de la aplicación.
Docker Compose es una herramienta que permite definir cómo se deben iniciar contenedores de Docker utilizando un
único archivo YAML\@.

En primer lugar se puede ver cómo se indica que se debe instanciar un contenedor con la base de datos, esto es,
utilizando una imagen de Postgres 18, en el servicio \texttt{postgres}.
En \texttt{environment} se indican las propiedades que deben ser reemplazadas en ejecución.
Estos valores están definidos en un archivo \texttt{.env} para centralizar las variables de configuración del entorno,
evitando dejarlas definidas en el código y permitiendo contar con más de un archivo \texttt{.env} para realizar el
despliegue de la aplicación en distintos ambientes (por ejemplo un ambiente local y otro en la nube).

Luego el servicio \texttt{ollama} se define a partir de la imagen Ollama 0.24.
Para la descarga a demanda del modelo LLM requerido se define otro servicio \texttt{ollama-init} que descarga el modelo
y luego finaliza su ejecución.

Por último el servicio \texttt{webapp} es el que sirve la aplicación web, tanto el \texttt{back-end} como el
\texttt{front-end}.
Como se puede apreciar, para que este servicio funcione, necesita que se le indiquen las credenciales de la base de
datos, la URL del servicio de Ollama, y la configuración de la API de Nominatim.

En todos los servicios se definen los puertos correspondientes en la sección \texttt{ports} y el volumen compartido
en la sección \texttt{volumes}.
Se agregaron verificaciones de \texttt{healthcheck} y \texttt{depends\_on} para que los servicios se inicien en el orden
correspondiente.
Esto evita por ejemplo que la aplicación web inicie antes de que la base de datos esté lista o para obtener un mensaje
cuando alguno de los contenedores finaliza su ejecución de forma inesperada.

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Archivo compose.yaml (se omiten varias líneas (...))},
    label={lst:docker-compose}
]
services:
  postgres:
    image: postgres:18-alpine
    (...)
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres:/var/lib/postgresql
  ollama:
    image: ollama/ollama:0.24.0
    (...)
    ports:
      - "${OLLAMA_PORT}:${OLLAMA_PORT}"
    volumes:
      - ollama:/root/.ollama
  ollama-init:
    image: ollama/ollama:0.24.0
    (...)
    entrypoint:
      - /bin/sh
      - -c
      - |
        ollama serve &
        server_pid=$$!
        until ollama list >/dev/null 2>&1; do
          sleep 1
        done
        ollama pull ${OLLAMA_MODEL}
        kill $$server_pid
        wait $$server_pid || true
        sleep "${OLLAMA_BOOTSTRAP_COOLDOWN_SECONDS}"
    volumes:
      - ollama:/root/.ollama
  webapp:
    image: extractor-webapp:latest
    build:
      context: .
    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - ./logs:/app/logs

volumes:
  ollama:
  postgres:
\end{lstlisting}

\subsubsection{Despliegue \textit{cloud}: Azure}

Como proveedor \textit{cloud} se decidió utilizar Microsoft Azure por experiencia y por contar con créditos de
estudiante para afrontar los costos del despliegue.
Azure ofrece además el producto Azure Container Apps de forma que se simplifica el \textit{deploy} de la aplicación
gracias a la existencia de una imagen de Docker con el código fuente correspondiente.

Para definir la infraestructura necesaria para hacer el \texttt{deploy} en la nube se utiliza Terraform.
Terraform es una herramienta de Infrastructure as Code (IaC) que permite definir y crear infraestructura (bases de
datos, aplicaciones, máquinas virtuales, redes, etc.) mediante archivos declarativos.
De esta forma se puede realizar el \texttt{deploy} completo con un único comando de terminal \texttt{terraform apply}
sin haber interactuado con la interfaz web o consola de la solución en la nube elegida.
Del mismo modo, con el comando \texttt{terraform destroy} se procede a la eliminación de todos los recursos creados.

La configuración Terraform del proyecto se encuentra en el directorio \nolinkurl{infra/terraform/azure-container-apps}
del módulo \texttt{extractor}.
Se cuenta con varios archivos de extensión \texttt{.tf} destacando el archivo \texttt{main.tf} donde se listan todos
los recursos que se deben construir en la nube para lograr el \textit{deploy}.
Tal como se puede apreciar en el \autoref{lst:terraform-main} se necesita crear un \textit{resource} para cada recurso.
Así se necesita un recurso para la instancia de PostgreSQL, otro para la aplicación web y varios recursos más que
se encargan de configurar esos recursos.

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={main.tf (se omiten varias líneas (...))},
    label={lst:terraform-main}
]
resource "azurerm_resource_group" "this" {
  name     = local.resource_group_name
  location = var.location
  tags     = local.merged_tags
}

resource "azurerm_postgresql_flexible_server" "this" {
  name                          = local.postgres_server_name
  resource_group_name           = azurerm_resource_group.this.name
  location                      = azurerm_resource_group.this.location
  version                       = var.postgres_version
  delegated_subnet_id           = azurerm_subnet.postgres.id
  private_dns_zone_id           = azurerm_private_dns_zone.postgres.id
  administrator_login           = var.postgres_admin_username
  administrator_password        = random_password.postgres_admin_password.result
  public_network_access_enabled = false
  backup_retention_days         = 7
  geo_redundant_backup_enabled  = false
  sku_name                      = var.postgres_sku_name
  storage_mb                    = var.postgres_storage_mb
  zone                          = "1"
  tags                          = local.merged_tags
  (...)
}

resource "azurerm_container_app" "webapp" {
  name                         = local.container_app_name
  container_app_environment_id = azurerm_container_app_environment.this.id
  resource_group_name          = azurerm_resource_group.this.name
  revision_mode                = "Single"
  workload_profile_name        = "Consumption"
  tags                         = local.merged_tags

  secret {
    name                = "postgres-password"
    key_vault_secret_id = azurerm_key_vault_secret.postgres_password.versionless_id
    identity            = azurerm_user_assigned_identity.container_app.id
  }

(...)
}
\end{lstlisting}

Todos los recursos forman parte de un único \textit{resource group}.
En la \autoref{fig:azure-resources} se muestra una captura del portal de Azure donde se listan los 13 recursos
necesarios para el funcionamiento de la solución en la nube, pertenecientes al \textit{resource group}
\texttt{email2trip-demo-rg}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.00\textwidth]{images/azure-resources}
    \figcaption{Lista de recursos del \textit{resource group} \texttt{email2trip-demo-rg}}%
    \label{fig:azure-resources}
\end{figure}

\FloatBarrier{}

Se define una \textit{virtual network} con dos \textit{subnets} dedicadas: una para el entorno de Azure
Container Apps y otra para Azure Database for PostgreSQL Flexible Server.
La separación en dos subredes corresponde a que cada servicio requiere una delegación distinta logrando
aislamiento entre la capa de aplicación y la capa de datos.

Se cuenta con un \textit{Container Apps Environment} integrado a la red virtual y, dentro de él, una
\textit{Container App} pública para servir la aplicación web Spring Boot.
La imagen del contenedor se descarga desde un repositorio privado de Docker Hub creado para tal fin.
En el \autoref{lst:docker-buildx} se lista el comando necesario para crear una imagen de Docker con todo el código
fuente del módulo \texttt{server} de extractor y cargarla en el repositorio privado de Docker Hub.

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Comando para crear y publicar la imagen de Docker del \texttt{server} en un repositorio privado.},
    label={lst:docker-buildx}
]
docker buildx build \
  --platform linux/amd64 \
  -t "docker.io/${DOCKERHUB_USERNAME}/extractor-webapp:latest" \
  . \
  --push
\end{lstlisting}

En cuanto a la DB se utilizó Azure Database for PostgreSQL Flexible Server.
Se despliega la DB dentro de una \textit{subnet} delegada, deshabilitando así el acceso público y definiendo una
\textit{Private DNS Zone} para que la aplicación web resuelva el nombre del servidor dentro de la red virtual.

También se utiliza Azure Key Vault para el almacenamiento de secretos de ejecución.
Allí se almacena la contraseña aleatoria autogenerada para PostgreSQL, el token de acceso a la imagen de
Docker Hub y la API key de Ollama Cloud.
Para acceder a esos secretos se define una \textit{User Assigned Managed Identity} asociada a la Container App.

Para el sistema de recomendaciones cabe mencionar que a diferencia del despliegue local con Docker se utiliza
directamente el servicio \textit{cloud} de Ollama.
Ollama ofrece \texttt{gemma4:31b-cloud} el cual es un modelo con más parámetros que el utilizado de forma local y
permite un uso gratuito a través de la API de Ollama Cloud con topes que exceden el uso esperado para una demostración
del proyecto.

El resto de los recursos corresponden a componentes de monitoreo y control de costos.
Se crea un \textit{Log Analytics Workspace} para centralizar logs y métricas del entorno.
En la \autoref{fig:azure-log} se puede apreciar la salida de la terminal de la aplicación de Spring Boot en
funcionamiento, tal como si se estuviera ejecutando localmente.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.00\textwidth]{images/azure-log}
    \figcaption{\textit{Log stream} de la aplicación Spring Boot desplegada en \textit{Azure Container Apps}}%
    \label{fig:azure-log}
\end{figure}

\FloatBarrier{}

Para el seguimiento presupuestario se define un \textit{budget} mensual sobre el \textit{resource group}, con
notificaciones al sobrepasar umbrales de gasto real o proyectado.
También se define un \textit{Action Group} de Azure Monitor y alertas de uso de CPU y memoria tanto para la Container
App y CPU y disco para la DB\@.
Estas alertas permiten detectar tempranamente si la aplicación queda subdimensionada o si la base de datos presenta
saturación durante la prueba.

Además del código fuente de la infraestructura propiamente dicho, se define un \textit{backend} remoto de Terraform
utilizando Azure Blob Storage.
Para ello se crea un \textit{resource group} específico para el estado, un \textit{storage account} y un
\textit{container} donde se almacena el archivo \texttt{tfstate}.
Esto permite persistir el estado fuera de la máquina local, facilitando la continuidad del trabajo entre
sesiones y reduciendo el riesgo de inconsistencias entre el estado real de Azure y el estado manejado por Terraform.

Respecto al manejo de secretos, se evitó contar con credenciales sensibles escritas en archivos de Terraform.
En su lugar se utilizó 1Password CLI (\texttt{op}) como herramienta de obtención de secretos en ejecución.
En una bóveda llamada \texttt{email2trip} se almacenan el nombre de usuario y el \textit{personal access token} de
Docker Hub y la API key de Ollama Cloud.
Antes de ejecutar los comandos necesarios de Terraform, estos valores se exportan a variables de entorno del tipo
\texttt{TF\_VAR\_*} (Ver \autoref{lst:op-tf}) de forma que Terraform los consuma sin necesidad de persistirlos en el
código del proyecto.

\begin{lstlisting}[
    style=jsonlikecodestyle,
    caption={Comandos para exportar a variables de entorno los secretos almacenados en 1Password.},
    label={lst:op-tf}
]
export TF_VAR_registry_password="$(op read 'op://email2trip/dockerhub/pat')"
export TF_VAR_container_image="docker.io/${DOCKERHUB_USERNAME}/extractor-webapp:latest"
export TF_VAR_ollama_cloud_api_key="$(op read 'op://email2trip/ollama-cloud/api_key')"
\end{lstlisting}

Finalmente, las instrucciones para realizar un \textit{deploy} desde cero se encuentran en un archivo \texttt{README}.
De esta forma el despliegue queda automatizado y reproducible.

En la \autoref{fig:azure-diagram} se detalla el diagrama de arquitectura correspondiente.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.00\textwidth]{images/azure-diagram}
    \figcaption{Diagrama de Arquitectura}%
    \label{fig:azure-diagram}
\end{figure}

\FloatBarrier{}
